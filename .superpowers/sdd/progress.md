# TorqueFlow Phase 1 — Progress Ledger

## Incident 2026-08-09
Task 2 implementer (haiku) worked around npm case-sensitivity naming rule by scaffolding in /tmp and copying back, which wiped .git, docs/, .atl/ and reinitialized git on branch "master". Recovered docs/design doc from conversation context; docs/superpowers/plans/2026-08-09-torqueflow-phase1-nucleo.md (full 17-task detail) is UNRECOVERABLE and must be regenerated via writing-plans before Task 3 can proceed. Task 1 and Task 2 redone with explicit no-delete safeguards.

Task 1: complete (root commit 4715f2e on main, pushed to origin/main, https://github.com/alejozd/torqueflow.git)
Task 2: complete (commits 4715f2e..9f7433e, pushed to origin/main, review clean — safe scratch-dir scaffold procedure verified docs/.gitignore/README untouched)
Task 3: complete (commit c07eb28, pushed to origin/main, review APPROVED — Vitest/RTL/Playwright stack, TDD red/green verified, no protected files touched)

Task 4: complete (commits a1f58fd..c699d7c, pushed to origin/main, review APPROVED after one fix loop). Real remote Postgres now live: 192.168.1.50:5432, db torqueflowdb, user utorqueflow, CREATEDB granted by user 2026-08-10. Prisma pinned to exact 6.19.3 (not caret) — binding for all future Prisma tasks (5, 6, 8, 9, ...); Prisma 7 evaluated and explicitly declined by user due to project-wide ESM/Node>=20.19 migration risk, recorded in plan's Global Constraints. Migration `20260810190757_init_tenant` applied; `tenants` table live in `public` schema. `publicDb` client singleton at src/lib/db/public-client.ts.
Task 5: complete (commit 15b814d, pushed to origin/main, review APPROVED first pass — no fixes needed). Tenant schema template (`Usuario`/`Cliente`/`Vehiculo`/`HistorialVehiculo` + `Role` enum) live via migration `20260810191447_init_tenant_tables`, replayed against reference schema `taller_dev_reference` and fixture schema `test_task5_fixture`. `getTenantDb(schemaName)` factory at src/lib/db/tenant-client.ts, cached per schema.
Task 6: complete (commit edb4361, pushed to origin/main, review APPROVED first pass — no fixes needed). `provisionTenant`/`seedTenantUser` at scripts/provision-tenant.ts and scripts/seed-tenant-user.ts (SAFE_IDENTIFIER SQL-injection guard verified), CLI wrappers at scripts/cli/*.ts, npm scripts tenant:provision/tenant:seed-user. Manual CLI end-to-end verified against live DB, cleaned up. bcryptjs/tsx added; Prisma pin untouched at exact 6.19.3.

Task 7: complete (commits c07eb28..a1f58fd, pushed to origin/main, review APPROVED — pure Edge-safe extractTenantSlug, 8/8 tests, no DB dependency)

Task 8: complete (commit de0aff4, pushed to origin/main, review APPROVED first pass — no fixes needed). src/middleware.ts (Edge layer, header tagging) + resolveTenant() at src/lib/tenant/resolve-tenant.ts (Node layer, DB lookup), shared TENANT_SLUG_HEADER constant, design note at docs/design/notes/2026-08-09-tenant-resolution-edge-split.md. Edge-safety independently verified (middleware.ts's transitive import closure has zero Prisma/Node-only deps).
  KNOWN DEBT (non-blocking, tracked): Next.js 16.3.0 deprecated `middleware.ts` in favor of `proxy.ts` (function `proxy` instead of `middleware`). Confirmed still fully functional (dev server compiles/executes it per-request, only a console deprecation warning, both filenames recognized in Next's build-detection code) — not fixed now because 9 more plan tasks (9, 12, 17, ...) reference `middleware.ts` by name and a mid-plan rename would ripple. Consider running `npx @next/codemod@canary middleware-to-proxy .` as a cleanup pass once Phase 1 is feature-complete, before or shortly after Task 17. Also: Next 16's Proxy now defaults to Node.js runtime (not strictly Edge) — the Edge/Node split's original *capability* rationale is technically looser in this Next version, but Next's own docs still discourage DB work in Proxy for other reasons, so the split architecture stays valid and unchanged.

Task 9: complete (commit 5cb5fcb, pushed to origin/main, review APPROVED first pass — no fixes needed, security-scrutinized). next-auth@5.0.0-beta.32 (npm @latest still resolves v4, no promotion). Tenant-scoped Credentials provider at src/auth.ts: authorize() calls resolveTenant() before any DB lookup, scopes via getTenantDb(tenant.schemaName), verifies via bcrypt.compare in src/lib/auth/verify-credentials.ts. Session/JWT carry role/tenantSlug/tenantSchema, typed via src/types/next-auth.d.ts. Route handler at src/app/api/auth/[...nextauth]/route.ts. Tenant-isolation boundary independently verified fail-closed by reviewer.

Task 10: complete (commit 7638ef7, pushed to origin/main, review APPROVED first pass — no fixes needed). requireSession/requireRole guards at src/lib/auth/guards.ts, requireRole fails closed through requireSession first before checking role. Redirects to /login and /login?error=forbidden.

Task 11: complete (commit ab0c973, pushed to origin/main, review APPROVED first pass — no fixes needed). /login route: src/app/login/page.tsx + LoginForm client component at src/app/login/login-form.tsx (signIn("credentials", ...) client-side, redirects to /clientes on success, shows role="alert" error on failure). This is the redirect target for Task 10's requireSession and Task 17's e2e flow start.

Task 12: complete (commit 702b9d4, pushed to origin/main, review APPROVED first pass — no fixes needed, authorization-scrutinized). Cliente CRUD server actions at src/app/actions/cliente-actions.ts, zod validation at src/lib/validation/cliente.ts, (dashboard) route-group layout guard. Authorization split independently verified: writes (create/update/delete) require requireRole(["ADMIN","RECEPCION"]), reads (list/get) require only requireSession(); every DB call routes through tenantDbOrThrow() -> resolveTenant() -> getTenantDb(), no hardcoded schema anywhere.
  SEPARATE FIX (commit 11dfc3c, also pushed): discovered during Task 12's tsc --noEmit check — src/lib/auth/guards.ts (Task 10 deliverable, never had tsc run against it) had `Session = NonNullable<Awaited<ReturnType<typeof auth>>>`, which resolves to the WRONG overload of NextAuth v5's multi-purpose `auth` export (picks the NextMiddleware overload, not the zero-arg Session-returning one) — broke `tsc --noEmit` project-wide, though runtime behavior was unaffected (mocked tests never exercised the real type). Fixed by importing `Session` directly from `next-auth` (uses the module augmentation from Task 9's src/types/next-auth.d.ts) instead of deriving it. Verified: tsc --noEmit clean, full suite 31/31 still passing. Lesson: tsc --noEmit should probably be a standard step in every task from here on, not just ones that happen to mention it in the brief.

Task 13: complete (commit ad8f707 + fix commits de0d67e, e0097e0, pushed to origin/main, review APPROVED after one fix loop). /clientes list page + NuevoClienteForm at src/app/(dashboard)/clientes/. Two findings from first review, both fixed and re-verified:
  1. Implementer's vitest.setup.ts change (global afterEach(cleanup) for RTL) was only in the uncommitted working tree, not in the original commit — meant the delivered code couldn't pass its own tests on a clean checkout. Now committed separately as de0d67e ("test: add global RTL cleanup to vitest.setup.ts") — safe, standard RTL pattern, no-op for non-render tests, obsoletes Task 11's local cleanup boilerplate (left in place, not removed, since that was optional).
  2. Original commit removed `required` from the Nombre input to work around native HTML5 validation blocking form submission before React 19's server-action form fires (a real conflict between the brief's own Step 1 test and Step 3 code — also a genuine production UX bug if left as `required` with no noValidate, not just a test artifact). Fixed via e0097e0 to use `<form noValidate>` while restoring `required` on the input, preserving accessibility semantics.
  Also: chore commit 6e4eb4e added `*.tsbuildinfo` to .gitignore — tsc --noEmit with `incremental: true` in tsconfig.json keeps generating this file as scratch noise across tasks now that tsc is a standard per-task check.

Task 14: complete (commit 253ff43, pushed to origin/main, review APPROVED first pass — no fixes needed, authorization-scrutinized). Vehiculo CRUD server actions at src/app/actions/vehiculo-actions.ts, zod validation at src/lib/validation/vehiculo.ts, scoped to a parent Cliente via clienteId. Authorization split verified: writes (create/delete) require requireRole(["ADMIN","RECEPCION"]), reads (list/get) require only requireSession(); all DB access via tenantDbOrThrow(). One disclosed, controller-independently-verified deviation: `formData.get(...) ?? ""` coercion on placa/marca/modelo, needed because zod 4.4.3's .min(1) gives a generic type error (not the custom message) for `null` input vs `""` — confirmed via direct zod repro — brief's own test left the field entirely unset (-> null), so the brief's literal code couldn't have passed its own test without this fix.

Task 15: complete (commit 66261bf, pushed to origin/main, review APPROVED first pass -- verbatim match, no fixes needed). /clientes/[id] detail page + NuevoVehiculoForm at src/app/(dashboard)/clientes/[id]/. No brief/test conflict this time (test fills all required fields before submit, unlike Task 13/14 edge cases) -- reviewer independently verified this rather than trusting the "no conflicts" claim. Correctly relies on vitest.setup.ts global cleanup, no redundant local cleanup added.
Task 16: complete (commit 5a0db78, pushed to origin/main, review APPROVED first pass -- verbatim match, security-scrutinized). HistorialVehiculo server actions at src/app/actions/historial-actions.ts (three-role addHistorialEntryAction: ADMIN/RECEPCION/TECNICO, tecnicos can log their own work), zod validation at src/lib/validation/historial.ts, /vehiculos/[id] detail page + NuevaEntradaForm. Reviewer independently verified: autorId sourced exclusively from session.user.id (requireRole return value), never from formData -- no spoofing path. No brief/test conflicts this time.
Task 17: complete (commit 8042767, pushed to origin/main, review APPROVED). e2e/global-setup.ts + global-teardown.ts (provision/drop real taller_e2e_smoke tenant via Task 6 scripts against live Postgres), e2e/tenant-flow.spec.ts (full login -> Cliente -> Vehiculo -> Historial flow via real Chromium against http://taller-e2e-smoke.localhost:3000, tenant resolved via real Host header through the real middleware -- no test-only code paths). Deliberate-break sanity check produced a genuine Playwright locator timeout, confirming the spec exercises real app behavior. Controller independently re-ran full suite outside the review (npm test: 44/44; npm run test:e2e: 2/2 including landing.spec.ts from Task 3) and independently verified teardown leaves zero residue (queried publicDb directly post-run: no Tenant row, no schema). ALL 17 TASKS OF PHASE 1 NOW COMPLETE.

## Final whole-branch review (2026-08-10)
After all 17 tasks completed, dispatched a final whole-branch review (opus model) covering Tasks 3-17 as a single unit, per subagent-driven-development's post-plan gate. Found 2 Critical, cross-cutting findings that no single task's scoped review could have caught (each task's action file correctly implemented its own local checks; the gap was structural, spanning auth.ts + guards.ts + all three action files):

1. CRITICAL - Cross-tenant session bypass: src/auth.ts minted tenantSlug/tenantSchema into the session/JWT at login, but nothing ever read it back to validate against the current request's resolved tenant. Every server action derived its tenant purely from the current Host header. Since all tenants share one AUTH_SECRET, a session token from tenant A would decrypt validly and pass requireSession()/requireRole() on tenant B's subdomain, then operate on tenant B's data. Controller independently verified via grep (tenantSchema never read outside auth.ts) before proceeding.
2. CRITICAL - trustHost unset: NextAuth v5 defaults trustHost=false outside dev/Vercel/CF-Pages; production auth on self-hosted infra would throw UntrustedHost on every request. Invisible in local dev.

FIXED in commit 1f0e3d1 (pushed to origin/main): requireSession() in src/lib/auth/guards.ts now also calls resolveTenant() and redirects to /login?error=tenant-mismatch if the resolved tenant's schemaName doesn't match session.user.tenantSchema -- this single choke point covers every action file since all of them route through requireSession()/requireRole() before touching the tenant DB (verified: zero unguarded getTenantDb() call sites). Added trustHost: true to the NextAuth() config in src/auth.ts (NOT AUTH_URL, which would break per-tenant host handling). New tests in guards.test.ts prove the mismatch redirect (RED confirmed against pre-fix code, GREEN after). Independently re-reviewed (opus, adversarial, re-traced the full attack path and all DB-access call sites, ran tsc/tests independently) -- verdict: Resolved, Ready to merge: Yes.

Non-blocking residual items flagged by the fix's re-review for a future hardening pass (Phase 2 or before real production deploy):
- R1: tenantDbOrThrow() in each action file independently re-derives the schema from resolveTenant() again rather than trusting the now-validated session -- two sources of truth that happen to agree today but aren't structurally forced to. Recommended: collapse to a single getTenantDb(session.user.tenantSchema) sourced from the guard's return value, deleting the duplicated tenantDbOrThrow() helper from all three action files.
- R2: redirect()'s NEXT_REDIRECT error is swallowed by try/catch in cliente-actions.ts's createCliente/updateCliente (calls requireRole inside the try) -- a tenant-mismatched or forbidden user sees literal "NEXT_REDIRECT" text instead of being redirected. Fails closed (no DB write happens), so UX bug not security bug. vehiculo-actions.ts/historial-actions.ts don't have this bug (guard called outside try).
- R3: /login?error=tenant-mismatch and ?error=forbidden are never displayed by the login page/form -- locked-out user sees a bare form with no explanation.
- R4: BASE_DOMAIN silently defaults to "zdevs.uk" in middleware.ts if unset -- combined with the new strict guard, a missing/misconfigured BASE_DOMAIN in prod would be a total authenticated-user lockout. Consider failing fast at boot if unset in production.
- R5: local .env (gitignored) contains AUTH_URL="http://localhost:3000", pre-existing and harmless in dev, but a footgun if ever copied into a prod env file (would break per-tenant host handling) -- worth removing or noting not to carry it forward.

Other Important/Minor findings from the whole-branch review (not fixed, deferred as Phase 2 backlog, none blocking):
- Raw Prisma error messages (P2002/P2003) leaked verbatim to end users in all three action files' catch blocks.
- HistorialVehiculo cascade-deletes when its parent Vehiculo is deleted (no confirmation/soft-delete) -- deleteVehiculoAction erases service history with one call; deleteClienteAction/deleteVehiculoAction are also unreachable from any UI this phase (never wired to a button).
- tenant-client.ts's client cache is a bare Map (no globalThis singleton pattern like public-client.ts, no eviction, no $disconnect) -- dev HMR leak risk and unbounded connection-pool growth in prod.
- Test suite isn't fully hermetic: tenant-client.test.ts depends on a manually-provisioned fixture schema; no test exists for auth.ts itself; no negative-authorization test existed before this fix (systemic gap in the task-brief template, not just this codebase).
- autorId on HistorialVehiculo has no FK relation to Usuario (dangling string), so it can't be joined to a display name.
- provisionTenant isn't atomic (orphaned schema possible on duplicate slug) and doesn't validate the slug against extractTenantSlug's reserved-word/format rules.
- src/app/layout.tsx still has the default create-next-app title/description.
- No sign-out UI anywhere in (dashboard).

PHASE 1 STATUS: All 17 planned tasks + both Critical post-review fixes are complete, reviewed, and pushed to origin/main. Ready for superpowers:finishing-a-development-branch.

## Backlog work (post-Phase-1, 2026-08-11)
Combined fix for backlog Tasks #18/#19/#21 in commit fa38900 (pushed to origin/main), independently re-reviewed (opus, adversarial, verified all 11 DB-touching functions across the 3 action files function-by-function) -- verdict Resolved, Ready to merge: Yes.
- #21 (R1 defense-in-depth): removed tenantDbOrThrow()/resolveTenant() from cliente-actions.ts/vehiculo-actions.ts/historial-actions.ts entirely. Every DB-touching function now derives its tenant schema exclusively from the already-validated session (getTenantDb(session.user.tenantSchema)), captured immediately after calling requireSession()/requireRole(). Structurally impossible now for an action to reach a tenant DB without a validated session in between -- confirmed zero remaining resolveTenant/tenantDbOrThrow references anywhere in the action layer.
- #19 (R2): fixed cliente-actions.ts's createClienteAction/updateClienteAction -- guard + DB-client derivation now happen before the try block (matching vehiculo-actions.ts/historial-actions.ts's already-correct pattern), so a redirect() from requireRole (tenant-mismatch or forbidden) propagates uncaught instead of being caught and rendered as literal "NEXT_REDIRECT" text.
- #18: added src/lib/db/prisma-error-message.ts (friendlyPrismaErrorMessage) mapping Prisma P2002/P2003 codes to Spanish messages, used in all three action files' catch blocks instead of leaking err.message verbatim. Also applied the user's chosen delete policy: HistorialVehiculo.vehiculo onDelete changed from Cascade to Restrict via migration 20260811010431_restrict_historial_delete (matches existing Vehiculo.cliente Restrict pattern) -- a Vehiculo with Historial entries can no longer be deleted. deleteClienteAction/deleteVehiculoAction (still unreachable from any UI, unchanged) now wrap their delete call in try/catch and re-throw with a friendly message instead of letting raw Prisma errors propagate.
Residual test-coverage gaps flagged by the re-review (rolled into task #25, not blocking): no test asserts getTenantDb was called WITH session.user.tenantSchema specifically (mocks ignore the argument); no regression test locks in the redirect-swallow fix; the 3 new friendly-error tests use plain-object fixtures rather than real PrismaClientKnownRequestError instances, so they wouldn't catch a regression back to raw err.message; delete actions still have zero test coverage and zero UI callers.

Trivial fix also pushed: commit 07c2427 -- src/app/layout.tsx metadata (title/description) changed from create-next-app defaults to TorqueFlow branding.

Remaining backlog tasks (tracked in session task list #20, #22, #23, #24, #25):
- #20: harden tenant-client.ts connection lifecycle (globalThis singleton pattern, connection_limit, eviction) -- not yet started.
- #22: fail fast on missing BASE_DOMAIN in production -- not yet started.
- #23: surface /login?error=tenant-mismatch and ?error=forbidden query params in the UI, add sign-out affordance -- not yet started.
- #24: add Usuario FK relation for HistorialVehiculo.autorId; harden provisionTenant (atomicity, slug validation against reserved words) -- not yet started.
- #25: test hermeticity -- auth.ts has zero tests, no negative-authorization test exists anywhere, tenant-client.test.ts depends on a manually-provisioned fixture; plus the residual gaps noted above from this session's refactor -- not yet started.

Task #20 done: commits 8a04f0c + ccdd0d7, pushed to origin/main, review APPROVED after one small fix loop. tenant-client.ts now mirrors public-client.ts's globalThis singleton pattern (dev-HMR-safe), each tenant client's Postgres pool bounded via connection_limit query param (TENANT_DB_CONNECTION_LIMIT env var, default 5), and a new framework-agnostic LruCache class (exported, unit-testable) caps total cached clients at MAX_CACHED_TENANT_CLIENTS=20 with $disconnect() on eviction. Total connections now bounded to roughly 20*5=100 regardless of how many tenants are touched over the app's lifetime, vs. previously unbounded. Fix loop closed a test-coverage gap: disconnectEvictedClient (the actual eviction->disconnect wiring, not just the generic LRU mechanics) is now directly unit-tested.

Task #22 done: commit ecb740c, pushed to origin/main (controller-authored directly, trivial mechanical 3-line guard, verified with tsc+full suite, no subagent dispatch needed). src/middleware.ts now throws at module load if BASE_DOMAIN is unset AND NODE_ENV=production, converting a silent misrouting/lockout risk into a loud, immediate deployment-time failure instead of a mysterious runtime login-loop discovered later.

Task #23 done: commit a74af32, pushed to origin/main, review APPROVED first pass -- no fixes needed. login-error-message.ts (pure getLoginErrorMessage mapper) + src/app/login/page.tsx now reads and displays ?error=tenant-mismatch/forbidden via role="alert". sign-out-button.tsx (client component, signOut({callbackUrl: "/login"})) rendered from (dashboard)/layout.tsx, which now captures and displays the session (email + tenantSlug) instead of discarding it. Reviewer independently confirmed e2e/tenant-flow.spec.ts locators don't collide with the new layout elements (no strict-mode risk).

Task #24 done: commits e2a4a82, 745eac7, 57f0db5, pushed to origin/main, review APPROVED after one small fix loop.
- Usuario<->HistorialVehiculo FK relation added (autor, onDelete: SetNull -- deliberately different from the vehiculo relation's Restrict, since losing a staff member shouldn't block their deletion or destroy service records, just null out the attribution). listHistorial now includes { autor: true }; vehiculos/[id]/page.tsx displays the author's name (or "Desconocido" when null).
- provisionTenant hardened: new isValidTenantSlug() in src/lib/tenant/subdomain.ts (reuses the existing RESERVED_SUBDOMAINS set, doesn't duplicate it, doesn't touch extractTenantSlug), called before any side effect; pre-check for an existing Tenant row by slug OR schemaName before CREATE SCHEMA; cleanup (DROP SCHEMA) on failure without masking the original error.
- Fix loop: first review found a real race condition in the cleanup logic -- two concurrent provisionTenant() calls for the same slug could have the losing call's cleanup destroy the winning call's just-created, legitimate schema. Fixed by re-checking for an existing Tenant row immediately before DROP SCHEMA in the catch block; only cleans up if no Tenant row exists (meaning nobody else won the race). Re-reviewed and confirmed resolved, test genuinely exercises the race path (not just the original single-caller case).

ALL 8 BACKLOG ITEMS FROM THE FINAL PHASE-1 WHOLE-BRANCH REVIEW ARE NOW COMPLETE except #25 (test hermeticity), which remains open.

Task #25 done: commits 78d2fa9, ac52e9f, 4b9b923, ef96a95, pushed to origin/main, review APPROVED after one small fix loop (opus, thorough final gate).
- Finding A: extracted authorize() logic from src/auth.ts into src/lib/auth/authorize-credentials.ts (authorizeCredentials, pure delegation, byte-identical behavior verified by reviewer against the pre-image), 6 test cases covering malformed credentials, unresolved tenant, wrong credentials, and exact-shape success (the success test's toEqual on a full object literal doubles as a security assertion: proves passwordHash from the mocked Usuario does NOT leak into the returned user object).
- Finding B: one negative-authorization test added to each of cliente-actions.test.ts/vehiculo-actions.test.ts/historial-actions.test.ts -- mocks requireRole to reject, asserts the action itself rejects (not swallowed) AND the DB mock is never called. Closes the systemic gap that let the original cross-tenant bypass ship past 15 individual reviews.
- Finding C: tenant-client.test.ts now self-provisions its own fixture schema (test_tenant_client_fixture) via a real provisionTenant() call in beforeAll, tears down in afterAll -- zero manual setup steps required on a fresh checkout.
- Fix loop: reviewer caught that the testTimeout bump (needed for concurrent live-DB provisioning contention across 3 test files) missed hookTimeout, leaving beforeAll-based provisioning still capped at Vitest's 10s default -- exactly where the new contention landed. Fixed (hookTimeout: 20000 alongside testTimeout: 20000). Also added missing "getTenantDb never called" assertions to authorizeCredentials' 4 early-return test cases (a Minor finding, folded into the same fix since it was cheap and directly related).

======================================================================
PHASE 1 + POST-REVIEW HARDENING: FULLY COMPLETE (2026-08-11)
======================================================================
All 17 planned tasks + 2 critical post-review security fixes + 8 backlog hardening items are implemented, individually reviewed (task-scoped gates) and independently re-verified where fixes were needed, and pushed to origin/main. Final state: 89/89 tests passing, tsc --noEmit clean, full e2e smoke test passing against the real stack.

Remaining known, accepted, non-blocking technical debt:
- middleware.ts uses Next.js 16's deprecated file convention (should migrate to proxy.ts eventually; confirmed still fully functional).
- Negative-auth test coverage is create-action-only per CRUD file (update/delete actions are structurally safe -- guard outside try, verified -- but untested against this specific regression class).
- tenant-client.test.ts's cached PrismaClient is never $disconnect()ed before its afterAll drops the schema (pre-existing pattern, low risk).
- A minor a11y polish item: the dashboard header's session/sign-out area has no semantic landmark.
- Pricing/plan tiers (design doc §9) and exact maxUsuarios limits remain open decisions, explicitly deferred by the user early in the design phase, do not block anything.


======================================================================
# TorqueFlow Fase 2 (Ordenes de Trabajo + DVI) -- Progress Ledger
======================================================================
Started 2026-08-20. Plan: docs/superpowers/plans/2026-08-20-torqueflow-phase2-ordenes-dvi.md (14 tasks). Executing via subagent-driven-development directly on main (explicit user consent, same as Fase 1 -- no worktree). Operational rules for this phase: max 1 fix/re-review loop per task (no unlimited retries), commit+push per task immediately, concise reports.

Task 1: complete (commits bb04e35..a2f2c98, pushed to main, review APPROVED after one fix loop). Sede/EstadoOrden/OrdenTrabajo/ItemOrden/ManoDeObra models live via migration, provisionTenant auto-creates one default Sede ("Sede principal") per new tenant. Fix loop closed a real orphan-Tenant gap: if sede.create() failed after tenant.create() succeeded, cleanup logic used to skip DROP SCHEMA (saw the Tenant row already existed) leaving a Tenant with zero Sede rows. Now tenant.create()'s row is deleted before re-throwing on sede-creation failure, verified by a new real-Postgres regression test (8/8 passing).
  RESIDUAL (non-blocking, backlog): re-review flagged a narrower compounding-failure edge case — if the compensating tenant.delete() itself throws, the original error is masked and the same orphan symptom can still occur. The outer catch block already has the correct pattern (try/catch + log + re-throw original err) three lines below; the inner catch just needs to mirror it. Not fixed now (2-failure scenario, out of original finding's scope, reviewer marked task Approved) — track for future hardening pass alongside other TorqueFlow backlog items.

Task 2: complete (commit 0e58de8, pushed to main, review APPROVED first pass -- verbatim match to brief, no fixes needed). src/lib/orden/estado-transitions.ts (ESTADO_ORDEN_TRANSITIONS map, isValidEstadoTransition) + src/lib/validation/orden.ts (ordenTrabajoInputSchema, itemOrdenInputSchema, manoDeObraInputSchema, estadoOrdenSchema). 7/7 tests passing.

Task 3: complete (commit 8cd3d74, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). src/app/actions/orden-actions.ts: listOrdenes/listOrdenesByVehiculo/getOrden/listTecnicos/createOrdenAction, ORDEN_DETAIL_INCLUDE (no dvi key yet, deliberately). 7/7 tests, tsc clean.

Task 4: complete (commit 05e4616, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). updateEstadoOrdenAction added to orden-actions.ts: validation->auth(ADMIN/RECEPCION/TECNICO)->lookup->isValidEstadoTransition check->update, entregadaAt/anuladaAt stamped only on their respective target states. 11/11 tests (7 Task3 + 4 new), tsc clean.

Task 5: complete (commit b7ee623, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). src/app/actions/item-orden-actions.ts: addItemOrdenAction (ADMIN/RECEPCION/TECNICO) + deleteItemOrdenAction (ADMIN/RECEPCION only). 4/4 tests.

Task 6: complete (commit 4c6bbc0, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). src/app/actions/mano-de-obra-actions.ts: addManoDeObraAction (ADMIN/RECEPCION/TECNICO) + deleteManoDeObraAction (ADMIN/RECEPCION only). 3/3 tests.

Task 7: complete (commit 9257317, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). /ordenes list page (estado filter allow-listed via ESTADOS_VALIDOS before reaching listOrdenes) + dashboard nav (Clientes/Ordenes links, purely additive to layout.tsx). No new tests (matches existing plain-RSC-page precedent); 116/116 full suite unchanged.

Task 8: complete (commit 1e36278, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). NuevaOrdenForm (folded mecanico/kilometraje/sintomas into one create form, "Sin asignar" default option) wired into vehiculos/[id]/page.tsx alongside the untouched Fase 1 Historial section (independently verified intact by reviewer via direct file read). 119/119 tests (3 new).

Task 9: complete (commit 5f601bd, pushed to main, review APPROVED first pass -- no fixes needed, deviation independently verified). Orden detail page (/ordenes/[id]) composed from CambiarEstadoForm (terminal-state static message vs select+button per ESTADO_ORDEN_TRANSITIONS), AgregarItemForm, AgregarManoObraForm -- add-only, no delete buttons wired (deliberate, matches deleteClienteAction/deleteVehiculoAction precedent). Implementer added noValidate to item/mano-de-obra forms (not in brief) to fix native HTML5 validation blocking the brief's own "shows error" tests -- same class of fix as Fase 1's e0097e0 (nuevo-cliente-form.tsx). Reviewer independently verified: claim is true (tests do submit with empty required fields), server-side Zod validation still fully enforces (noValidate only suppresses the browser popup), fix correctly scoped to only the two forms that needed it, required attrs preserved for a11y. 126/126 tests, tsc clean.

Task 10: complete (commit e8508dc, pushed to main, review APPROVED first pass -- no fixes needed). Dvi/DviFoto models + DviFotoMomento enum added (model deliberately named Dvi not DVI to avoid Prisma's awkward dVI client-property casing), dvi Dvi? relation added back to OrdenTrabajo, dviRealizados Dvi[] on Usuario. DVI_CHECKLIST_ITEMS (8 fixed keys) + DVI_CHECKLIST_STATUSES at src/lib/dvi/checklist-items.ts. orden-actions.ts correctly left untouched (ORDEN_DETAIL_INCLUDE dvi addition deferred to Task 13). 2/2 tests.

Task 11: complete (commit 475c282, pushed to main, review APPROVED first pass, security-scrutinized). src/lib/storage/local-file-storage.ts (saveDviFoto: 5MB limit, jpeg/png/webp only) + /api/uploads/[...path] route handler + next.config.ts serverActions.bodySizeLimit=6mb + .gitignore /uploads. Implementer proactively found and fixed a real cross-tenant bypass in the brief's own example code: requestedPath.startsWith(tenantRoot) (no trailing separator) let a sibling directory whose name string-prefixes the tenant schema (e.g. taller_perez-evil) pass via a plain unencoded ".." segment. Fixed with tenantRoot + path.sep. Reviewer independently reconstructed the exploit, confirmed the fix closes it fully (Windows and POSIX path.sep both correct, no regression on legitimate same-tenant access), and confirmed requireSession() already tenant-verifies the schema before the route trusts it. 3/3 tests, tsc/eslint clean.
  FLAG FOR TASK 12: reviewer noted saveDviFoto's write-side path.join(tenantSchema, dviId, filename) has no traversal guard (write side only, unlike the now-fixed download route) -- confirm in Task 12 review that dviId is always a DB-generated cuid from Prisma, never raw user/formData input, before it reaches saveDviFoto.

Task 12: complete (commit 78cc8c7, pushed to main, review APPROVED first pass, security-scrutinized). src/lib/validation/dvi.ts + src/app/actions/dvi-actions.ts: updateDviChecklistAction (ADMIN/RECEPCION/TECNICO, upserts Dvi, filters formData to only recognized DVI_CHECKLIST_ITEMS keys with valid statuses -- unrecognized/invalid silently dropped not erroring), addDviFotoAction (ADMIN/RECEPCION/TECNICO, requires Dvi row to exist first, "Primero guarda el checklist de inspeccion" otherwise), deleteDviFotoAction (ADMIN/RECEPCION only). Task 11's flagged security question resolved: reviewer independently traced both uses of dvi.id in addDviFotoAction back to tenantDb.dvi.findUnique's return value -- formData never contributes to the dviId that reaches saveDviFoto (which itself has no traversal guard, confirmed at local-file-storage.ts -- this provenance discipline is the only thing preventing write-side path manipulation). 5/5 tests, tsc clean.

Task 13: complete (commit 8f22ed2, pushed to main, review APPROVED first pass -- no fixes needed). DviChecklistForm + DviFotoForm mounted on orden detail page's new DVI section, alongside intact Task 9 sections (info/estado/items/mano de obra -- diff-stat confirmed zero deletions in page.tsx). orden-actions.ts's ORDEN_DETAIL_INCLUDE finally gets dvi: { include: { fotos: true } } (deferred since Task 3 until Dvi existed). img src correctly uses foto.url (auth-gated /api/uploads route, never raw path). Implementer caught 2 gaps in my own plan: (1) DviFotoForm's brief code was missing noValidate (needed for the required file input, matches 3 existing sibling-form precedents), (2) Task 13's brief Step 7 commit file list omitted orden-actions.ts even though Step 1 modifies it -- implementer included it correctly, reviewer confirmed it's a clean single-line addition. 140/140 tests.

Task 14: complete (commits a0015bf..2525b46, pushed to main, review APPROVED after one fix loop). e2e/tenant-flow.spec.ts extended through orden creation -> item -> mano de obra -> DVI checklist -> DVI foto upload -> estado BORRADOR->EN_PROCESO->TERMINADA->ENTREGADA. First implementer attempt hit an API quota error mid-task (no commits, resumed clean). Second attempt hit 2 genuine locator-ambiguity bugs (not app bugs): getByRole("status") matched 2 simultaneous status messages (fixed .filter), getByLabel("Frenos") substring-matched "Niveles de fluidos (...frenos)" (fixed with {exact:true}) -- both independently verified against real DOM by reviewer. Fix loop closed a real e2e false-positive: the DVI foto <img> toBeVisible() assertion would pass even if the auth-gated /api/uploads/[...path] route were broken (layout box exists regardless of load success). Fixed with page.waitForResponse asserting a real 200 from the uploads route, sequenced inside the same Promise.all as the triggering click (no race). Implementer verified the fix in BOTH directions: temporarily sabotaged the route handler with an unconditional 403, confirmed the new assertion actually fails, reverted, confirmed clean pass again -- reviewer independently confirmed route handler file is absent from the final diff (sabotage fully reverted) and the reported failure trace's line numbers match the committed code exactly. e2e 2/2, unit 140/140.

======================================================================
FASE 2 (ORDENES DE TRABAJO + DVI): ALL 14 TASKS COMPLETE (2026-08-20)
======================================================================
All 14 planned tasks implemented, task-scoped-reviewed (3 fix loops total: Task 1 orphan-tenant gap, Task 11 cross-tenant path-traversal bypass caught proactively by implementer, Task 14 e2e false-positive), pushed to main. Final whole-branch review pending next.

## Final whole-branch review (2026-08-20)
Dispatched final review (opus) over the full Fase 2 diff (5a20704..2525b46, 17 commits). Multi-tenant isolation, auth role matrix, and state machine all held cleanly across all 14 independently-reviewed tasks. Found 1 Critical + 7 Important + 11 Minor.

FIXED (user-approved) in commit bb19f35, re-reviewed (opus) and independently verified (162/162 tests, tsc clean):
- CRITICAL: listTecnicos() returned the full Usuario row (incl. bcrypt passwordHash) as a prop into NuevaOrdenForm, a client component -- serialized the hash to every authenticated browser. Fixed: listTecnicos now selects only {id, nombre} (new TecnicoOption type).
- Important: ORDEN_DETAIL_INCLUDE's mecanico:true was the same latent leak class (not yet exploited, server-only read) -- scoped to {id, nombre}.
- Important (user-approved to implement now, not defer): nothing blocked adding items/mano-de-obra/DVI-checklist/DVI-fotos to an ENTREGADA/ANULADA order. Added src/lib/orden/mutable-guard.ts (assertOrdenMutable), wired into all 7 mutation entry points (item add/delete, mano-de-obra add/delete, DVI checklist update, DVI foto add/delete), each with a test proving the block.
- Important: /api/uploads/[...path] (the phase's highest-risk code, already had one real path-traversal bug from Task 11) had zero test coverage -- added route.test.ts, 7 table-driven cases including a regression test for the exact Task 11 bypass class.
- Important: uploads route missing security headers (X-Content-Type-Options, Cache-Control) and unknown file extensions fell through to application/octet-stream instead of failing closed -- both fixed.
- Important: DVI photo MIME type trusted from client file.type with no byte verification -- added magic-byte checks (JPEG/PNG/WEBP signatures) before any disk write.

DEFERRED to backlog (user-confirmed, not blocking -- no real tenants in production yet):
- Important: no script to migrate schema/backfill default Sede for tenants provisioned before this phase. Needed before any real deploy with existing tenants, not needed today.

SECOND fix round: re-review surfaced one NEW Important finding (not present in round 1): the 3 delete actions (deleteItemOrdenAction/deleteManoDeObraAction/deleteDviFotoAction) checked assertOrdenMutable against the supplied ordenId but never verified the target record actually belonged to that order -- a privileged (ADMIN/RECEPCION) caller could delete a record on a terminal-state order by passing a mismatched non-terminal ordenId. User approved fixing immediately. Fixed in commit 52e40cc: item/mano-de-obra deletes changed to deleteMany({where:{id, ordenId}}) checking count===0; DviFoto (no direct ordenId scalar, relates via dvi.ordenId) uses the nested relation filter deleteMany({where:{id, dvi:{ordenId}}}) -- confirmed valid against the live Prisma schema. Agent-dispatch classifier blocked a subagent re-review of this final fix (twice) -- controller performed this review directly: read the full diff, confirmed the DviFoto relation field names against prisma/tenant/schema.prisma, and independently re-ran the targeted tests (22/22), tsc (clean), and the full suite (165/165, including real-DB integration tests) via Bash. Verdict: closes the gap correctly for all three actions.

Minor findings (11 total, #8-18 from round 1) NOT addressed -- deferred to backlog: validation-before-auth ordering (house style, low risk), silent-drop-invalid-checklist-key + full-replace semantics, orphaned file on DB-insert-failure after disk write (mirrors the already-accepted DviFoto-delete-doesn't-delete-file tradeoff), money as JS float via z.coerce.number() into Decimal columns (flag before Fase 4 billing), missing revalidatePath("/ordenes") on create/estado-change, inconsistent try/catch style in deleteDviFotoAction vs siblings, uneven requireRole-argument assertions across DVI tests, no defensive param validation in saveDviFoto, unvalidated JSON cast for checklist on the detail page, brittle e2e selectors (positional .first()/.nth()), mecanicoId not constrained to TECNICO role. Also noted: historial-actions.ts's include:{autor:true} is the same Usuario-select leak class, predates this phase (Fase 1), not in scope here -- add to the same "select not include for Usuario relations" backlog item as the two Fase 2 leaks above.

======================================================================
FASE 2 (ORDENES DE TRABAJO + DVI): COMPLETE, REVIEWED, READY TO MERGE (2026-08-20)
======================================================================
All 14 tasks + 2 review-driven fix rounds (Critical/Important batch, then the delete-ownership follow-up) implemented, reviewed, and pushed to main. Final state: 165/165 tests passing (unit + real-Postgres integration + e2e), tsc --noEmit clean, eslint clean on all touched files. No branch/PR (direct-to-main per established project convention). Next: Fase 3 (Inventario, repuestos y proveedores) per the design doc's Fase 2-9 roadmap.

## FASE 2 SUMMARY
- 14 tasks completed (final commit 52e40cc)
- 165/165 tests passing (unit + real-Postgres integration + e2e)
- 5 security findings found and resolved during the process (Task 1 orphan-tenant gap, Task 11 cross-tenant path-traversal, Task 14 e2e false-positive, final-review Critical passwordHash leak, final-review-2 delete-action IDOR)
- Technical debt documented: Sede migration/backfill script for tenants provisioned before this phase (backlog, not blocking -- no real tenants yet) + 11 Minor findings (see "Final whole-branch review" section above for the full list)
- Status: Fase 2 complete, ready for Fase 3


======================================================================
# TorqueFlow Fase 3 (Inventario, Repuestos y Proveedores) -- Progress Ledger
======================================================================
Started 2026-08-20. Plan: docs/superpowers/plans/2026-08-20-torqueflow-phase3-inventario.md (16 tasks). Executing via subagent-driven-development directly on main (explicit user consent, same as Fase 1-2 -- no worktree). Fase 3 RULES.md updates apply: commit format "fase3-task X: descripcion" (not just "task X"), do not touch Fase 1/2 backlog items during this phase, max 1 fix/re-review loop per task.

Task 1: complete (commit c0d9e30, pushed to main, review APPROVED -- controller performed this review directly, Agent-dispatch classifier blocked subagent review twice, same recurring issue as end of Fase 2). Bodega/Proveedor models live, provisionTenant reuses the existing Sede-creation try/catch (Fase 2's orphan-safety fix) to also create a default "Bodega principal" tied to the new Sede's id -- verified: same catch block, tenant.delete on any failure inside it. Migration SQL correct (bodegas.sede_id FK RESTRICT, indexed). No Repuesto/EntradaMercancia references (correctly deferred). New test mirrors the existing Sede test's real-DB pattern exactly. 9/9 tests passing.

Task 2: complete (commit bfadcb5, pushed to main, tsc clean). src/lib/validation/inventario.ts: bodegaInputSchema, proveedorInputSchema. NOTE: from this point, Fase 3 execution switched to INLINE (not subagent-driven) -- Claude Code auto-mode classifier began blocking ALL Agent dispatches (both implementer and reviewer) with generic "Blocked by classifier", confirmed after 2 retries each. User approved switching to inline execution for the rest of the phase, reaffirming the same operational rules (fase3-task N commit format, max 1 correction attempt, concise reports, tsc/tests at task end, stop near token limits).

Task 3: complete (commit bee3f04, pushed to main, inline execution). bodega-actions.ts CRUD. 1 correction applied: plan's exact code used formData.get("nombre") without ?? "" -- when the field is absent, FormData.get returns null, and zod 4.4.3's z.string().min(1) gives the generic type-error message ("Invalid input: expected string, received null") instead of the custom "El nombre es obligatorio" message for null specifically (same known class documented in Fase 1's ledger for placa/marca/modelo). Fixed both createBodegaAction and updateBodegaAction with formData.get("nombre") ?? "". REMEMBER: apply this same ?? "" pattern to every required-string formData.get(...) call in the remaining Fase 3 actions (proveedor nombre, repuesto codigo/nombre, entrada-mercancia proveedorId/bodegaId, item repuestoId etc.) instead of copying the plan's literal code verbatim. 6/6 tests.

Task 4: complete (commit d97cc4a, pushed to main, inline execution). /bodegas list page + NuevoBodegaForm + nav link. 174/174 full suite, tsc clean.

Task 5: complete (commit 9cd4d56, pushed to main, inline execution). proveedor-actions.ts CRUD, ?? "" pattern applied from the start (no correction needed). 5/5 tests, tsc clean.

Task 6: complete (commit e4bd58c, pushed to main, inline execution). /proveedores list page + NuevoProveedorForm + nav link. 182/182 full suite, tsc clean.

Task 7: complete (commit 73023e5, pushed to main, inline execution). Repuesto model added (codigo unique, bodegaId required RESTRICT, proveedorId optional SETNULL), repuestos back-relations on Bodega/Proveedor. Migration applied against real Postgres, tsc clean.

Task 8: complete (commit d7e8ecf, pushed to main, inline execution). repuestoInputSchema + repuesto-actions.ts CRUD. Confirmed z.coerce.number() fields (precioCompra/precioVenta/stockMinimo) don't need the ?? "" fix (Number(null)=0, no type-mismatch error like z.string() has) -- only applied ?? "" to plain z.string() fields (codigo, nombre, bodegaId, proveedorId, descripcion). stockActual write-once enforced structurally (absent from updateRepuestoAction's data object). 7/7 tests, tsc clean, no correction needed.

Task 9: complete (commit 93ea064, pushed to main, inline execution). /repuestos list page + NuevoRepuestoForm (bodega/proveedor selects) + passive "stock bajo" marker + nav link. 192/192 full suite, tsc clean.

Task 10: complete (commit 107af57, pushed to main, inline execution). EntradaMercancia + EntradaMercanciaItem models added (header+lines pattern mirroring OrdenTrabajo/ItemOrden; EntradaMercanciaItem.repuesto onDelete:Restrict unlike ItemOrden's SetNull). Back-relations added on Proveedor/Bodega/Repuesto/Usuario. Migration applied against real Postgres, tsc clean.

Task 11: complete (commit df4e266, pushed to main, inline execution). entradaMercanciaInputSchema + entradaMercanciaItemInputSchema + entrada-mercancia-actions.ts: createEntradaMercanciaAction (header) + addEntradaItemAction (atomic tenantDb.$transaction([itemCreate, repuestoUpdate-increment])) + listEntradas/getEntrada. 6/6 tests including atomicity/rejection test, tsc clean, no correction needed.

Task 12: complete (commit 48adbad, pushed to main, inline execution). /entradas-mercancia list page + NuevaEntradaMercanciaForm (proveedor/bodega selects) + nav link. 201/201 full suite, tsc clean.

Task 13: complete (commit 1663484, pushed to main, inline execution). /entradas-mercancia/[id] detail page + AgregarEntradaItemForm (repuesto select). 204/204 full suite, tsc clean.

Task 14: complete (commit d68c161, pushed to main, inline execution). ItemOrden.repuestoId (optional FK, SetNull) linked to Repuesto. itemOrdenInputSchema now .refine()-based (repuestoId OR manual descripcion+precioUnitario). addItemOrdenAction derives trusted descripcion/precioVenta server-side when repuestoId given. item-orden-actions.test.ts fully replaced (existing "creates item" test updated to expect repuestoId:null, 4 new tests added). 10/10 file tests, 207/207 full suite, tsc clean, ZERO corrections needed -- careful upfront design (schema refine ordering, ?? "" pattern) paid off.

Task 15: complete (commit 7b1c32a, pushed to main, inline execution). AgregarItemForm gets repuesto select ("Ítem manual" default + catalog options), descripcion/precioUnitario no longer required (server-refined). Orden detail page fetches listRepuestos() alongside getOrden(), passes to AgregarItemForm. 208/208 full suite, tsc clean.

Task 16: complete (commit 596414f, pushed to main, inline execution). e2e/tenant-flow.spec.ts extended: bodega creation -> proveedor -> repuesto -> entrada de mercancia (real stock increment via $transaction) -> existing Fase1/2 flow -> catalog-linked order item (trusted pricing) -> stock unchanged through order lifecycle to ENTREGADA.
  REAL BUG CAUGHT BY THIS E2E RUN (not caught by any unit test, since mocks never exercise real RSC/Client serialization): passing Repuesto[]/RepuestoWithDetalle[] (Decimal precioCompra/precioVenta fields) as props into "use client" components (AgregarEntradaItemForm from Task 13, AgregarItemForm from Task 15) crashed at runtime -- "Only plain objects can be passed to Client Components from Server Components. Decimal objects are not supported." Same root-cause class as Fase 2's final-review Critical (listTecnicos leaking full Usuario) but here it's a hard crash instead of a data leak. FIXED: added RepuestoOption {id, codigo, nombre} type + listRepuestoOptions() (select-only, no Decimal fields) to repuesto-actions.ts, mirroring the TecnicoOption/listTecnicos pattern from Fase 2's fix. Updated both AgregarEntradaItemForm's and AgregarItemForm's prop types + their pages to use listRepuestoOptions() instead of listRepuestos(). Also fixed 2 unrelated e2e bugs in the same pass (my own test code): selectOption({label: /regex/}) isn't valid Playwright API, needs an exact string -- changed both occurrences to "FRN-001 -- Filtro de aceite". 1 correction attempt, e2e 2/2 clean after, unit 208/208 unaffected.

======================================================================
FASE 3 (INVENTARIO, REPUESTOS Y PROVEEDORES): ALL 16 TASKS COMPLETE (2026-08-20)
======================================================================
All 16 tasks implemented (Tasks 1 inline-reviewed by controller after classifier blocked subagent review; Tasks 2-16 fully inline per user's approval after the classifier began blocking ALL Agent dispatches). 208/208 unit tests + 2/2 e2e passing, tsc clean. Final whole-branch review pending next (controller will perform it directly given the classifier situation).

## Final whole-branch review (2026-08-20, session 2)
Dispatched final review (opus) over the full Fase 3 diff (`c0d9e30~1..596414f`, 16 commits, +1981/-21). Agent dispatch worked this session (classifier did not block it, unlike the rest of the phase). Tenant isolation, Decimal/secret-leak guard, assertOrdenMutable placement, delete-ownership, stock write-once, `?? ""` formData pattern, role matrix, and Prisma error handling all traced clean with no regressions. Found 1 Critical + 3 Important (cross-cutting, spanning Tasks 7/8/10/11/13/14):

1. CRITICAL - Zero-price coercion: `precioCompra`/`precioVenta` used bare `z.coerce.number().min(0, ...)`. Since `NuevoRepuestoForm` has `noValidate`, a blank price input submits `""`, `Number("")` is `0`, so `.min(0)` silently accepted it. A part could be created with `precioVenta: 0`, invisible in the `/repuestos` list view (prices aren't displayed there), with no in-app repair path (`updateRepuestoAction` is wired to zero UI). That `0` then flows into order billing via Task 14's server-trusted-pricing path (`item-orden-actions.ts` derives `precioUnitario` from `repuesto.precioVenta`, ignoring any manual override) -- silent revenue loss reachable through ordinary UI use, no tampering required.
2. Important (I2): identical mechanism on `precioCompraUnitario` in entrada de mercancia -- blank input records the goods-receipt line at cost 0 while still incrementing stock correctly, permanently destroying the cost basis (no edit/delete UI for receipt items).
3. Important (I3): `addEntradaItemAction` incremented `repuesto.stockActual` by id with no check that the chosen Repuesto's `bodegaId` matches the EntradaMercancia's `bodegaId` -- the picker on the entrada detail page listed every repuesto in the tenant regardless of warehouse. A receipt logged "at Bodega norte" could silently increment a Bodega-principal part's stock instead, making per-warehouse stock unreconcilable with the receipt ledger. Genuine cross-task gap (Task 7 model + Task 10 model + Task 11 transaction + Task 13 page) that no single task's scoped review could see.
4. Important (I4): `listRepuestoOptions` (the select-only fix that resolved Task 16's Decimal-crash) had zero test coverage proving its `select`-only shape -- same bug class had already caused two production-shaped failures in this codebase (Fase 2 `listTecnicos` passwordHash leak, Fase 3 Task 16 Decimal crash).

FIXED in commit 8c45b54 (pushed to main):
- `src/lib/validation/inventario.ts`: new `requiredMoney(msg)` helper (`z.preprocess` mapping `""`/`null`/`undefined` to `undefined` before `z.coerce.number().min(0)`) applied to `precioCompra`, `precioVenta`, `precioCompraUnitario` -- blank now fails with a required-field error; `0`, decimals, and negative-rejection all still behave correctly.
- `src/app/actions/entrada-mercancia-actions.ts`: `addEntradaItemAction` now fetches the entrada's and repuesto's `bodegaId` before the `$transaction` and rejects with a friendly error on mismatch (or missing entrada/repuesto), never throws.
- `src/app/actions/repuesto-actions.ts`: `listRepuestoOptions(bodegaId?)` now accepts an optional warehouse filter; the entrada detail page passes `entrada.bodegaId` to scope its picker. The orden detail page's unscoped call was deliberately left unchanged (orders aren't tied to one warehouse; `addItemOrdenAction` never touches `stockActual`).
- New regression tests: blank-price rejection (repuesto + entrada-item actions), cross-bodega rejection (3 cases: entrada not found, repuesto not found, bodega mismatch), and `listRepuestoOptions`' exact `findMany` call shape (proven to fail against both the pre-fix code and a hypothetical `include`-based leak).
- 215/215 unit tests passing, tsc --noEmit clean.

Independently re-reviewed (opus, adversarial -- ran zod boundary tests against the real installed version, re-derived the exploit paths, ran tsc/vitest itself rather than trusting reported numbers, proved the new I4 tests actually discriminate old vs. new vs. a leaky-`include` implementation). Verdict: all 4 findings Resolved, no regressions. New Minor-only findings (misleading error message on negative prices sharing C1's message; whitespace-only string still coerces to 0, unreachable from a real `<input type="number">`; TOCTOU window between the I3 check and the transaction, judged negligible, no locking warranted) -- all deferred to backlog per RULES.md #7.

**Ready to merge: Yes.**

Minor findings from the review NOT addressed (deferred to backlog, none blocking):
- `revalidatePath` gaps: `addEntradaItemAction` misses `/entradas-mercancia`; `createRepuestoAction` misses `/ordenes/[id]` and `/entradas-mercancia/[id]`; `createBodegaAction`/`createProveedorAction` miss `/repuestos` and `/entradas-mercancia`. Stale dropdowns until a hard nav.
- `createBodegaAction` picks the tenant's oldest Sede via `findFirst({orderBy: createdAt asc})` with no selector -- mirrors the pre-existing `createOrdenAction` pattern, consistent-by-convention, more visible now that Bodega->Sede exists.
- Validation runs before the auth guard in all 5 new action files (convention-level, matches Fase 2's `orden-actions.ts`, not a Fase 3 regression).
- `updateBodegaAction`/`deleteBodegaAction`/`updateProveedorAction`/`deleteProveedorAction`/`updateRepuestoAction`/`deleteRepuestoAction` are exported and tested but wired to no UI (intentionally deferred).
- `entradaMercanciaItemInputSchema.cantidad` has no upper bound (Postgres INTEGER overflow caught by the generic fallback error).
- `proveedorInputSchema.email` uses the zod-4-deprecated `z.string().email()` (still works).
- The new C1/I2/I3 Minor cosmetic items from the fix's re-review (see above).

======================================================================
FASE 3 (INVENTARIO, REPUESTOS Y PROVEEDORES): COMPLETE, REVIEWED, READY TO MERGE (2026-08-20)
======================================================================
All 16 tasks + 1 review-driven fix round (1 Critical + 3 Important) implemented, reviewed, independently re-reviewed, and pushed to main. Final state: 215/215 unit tests passing, tsc --noEmit clean. No branch/PR (direct-to-main per established project convention). Next: Fase 4 (Facturacion y Pagos) per the design doc's roadmap.

## FASE 3 SUMMARY
- 16 tasks completed (final feature commit 596414f) + 1 review-fix commit (8c45b54)
- 215/215 unit tests passing (208 pre-review + 7 new from the fix round), 2/2 e2e, tsc clean
- Real bug caught by e2e (not unit tests, since mocks never exercise real RSC/Client serialization): passing Prisma Decimal fields as props into "use client" components crashed at runtime (Task 16) -- fixed with a select-only Option type + listRepuestoOptions(), mirroring Fase 2's listTecnicos/TecnicoOption fix for the same leak class
- 1 Critical + 3 Important findings from the final whole-branch review, all fixed and independently re-verified: zero-price coercion into order billing, cross-bodega stock increment, missing regression coverage for the Decimal-leak guard
- Technical debt documented: revalidatePath gaps on 4 create actions, oldest-Sede-only bodega creation, unwired update/delete UI for Bodega/Proveedor/Repuesto, plus the pre-Fase-3 backlog carried from Fase 1/2 (see their sections above)
- Status: Fase 3 complete, ready for Fase 4

======================================================================
# TorqueFlow Fase 4 (Facturación y Pagos) -- Progress Ledger
======================================================================
Started 2026-08-20. Plan: docs/superpowers/plans/2026-08-20-torqueflow-phase4-facturacion.md (8 tasks). Executing via subagent-driven-development directly on main (explicit user consent, same as Fases 1-3 -- no worktree). IVA fijo 19% confirmado por el usuario antes de escribir el plan.

Task 1: complete (commit 4667778, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). Factura/Pago models + EstadoFactura (PENDIENTE/PAGADA, sin ANULADA -- deliberado, no hay flujo de anulación en esta fase)/MetodoPago enums, back-relations on Usuario/Cliente/OrdenTrabajo. Factura.ordenId unique (1:1 con la orden), numero autoincrement independiente del id cuid, todos los FK salientes de Factura/Pago con onDelete: Restrict (registros financieros, mismo criterio que HistorialVehiculo/EntradaMercanciaItem). Reviewer confirmó migration.sql coincide 1:1 con el schema.

Task 2: complete (commit 2e37545, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). requiredMoney relocalizado a src/lib/validation/money.ts (inventario.ts ahora lo importa, sin duplicados, 20 tests preexistentes intactos). computeFacturaTotales (IVA_RATE=0.19 fijo, redondeo en cada paso) + assertOrdenFacturable en src/lib/factura/. facturarOrdenInputSchema.descuento opcional (blank=0) vs pagoInputSchema.monto obligatorio vía requiredMoney+refine(&gt;0) -- reviewer verificó ambos casos límite directamente contra el runtime real de zod, no solo los tests del diff. 224/224 tests, tsc clean.

Task 3: complete (commit 186019f, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed, atomicidad escrutinada). factura-actions.ts: crearFacturaAction (parse -> role -> orden lookup -> duplicado -> facturable -> totales -> descuento&lt;=subtotal ANTES de abrir la transacción -> $transaction con factura.create + decrementos de stock agrupados por repuestoId vía Map, un solo update por repuesto distinto) + listFacturas/getFactura. Reviewer trazó el código de la transacción línea por línea y confirmó atomicidad real (mismo tx client, mismo callback, rollback de Prisma ante cualquier throw) y que emitidaPorId viene de la sesión, nunca de formData. 9/9 tests con valores numéricos exactos, tsc clean.

Task 4: complete (commit d5ba362, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). /facturas list page (filtro por estado PENDIENTE/PAGADA, mismo patrón que /ordenes) + nav link. Sin tests nuevos (precedente establecido para páginas RSC planas). 233/233 tests, tsc clean.

Task 5: complete (commit e06da7d, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). ORDEN_DETAIL_INCLUDE gana factura: {select: {id, numero}} (select-only, no Decimal leak -- disciplina ya probada dos veces necesaria en este proyecto). GenerarFacturaForm (useActionState + useEffect/router.push, sin redirect() server-side -- evita deliberadamente la clase de bug NEXT_REDIRECT-tragado-por-try/catch de Fase 1 R2) wireado en la página de orden: link a factura existente > form si TERMINADA/ENTREGADA > mensaje instructivo. 233/233 tests, tsc clean.

Task 6: complete (commit 9843d8f, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed, atomicidad del guard de sobrepago escrutinada). pago-actions.ts: registrarPagoAction dentro de un solo $transaction -- updateMany({where: {id, saldoPendiente: {gte: monto}}, data: {decrement}}) -> count===0 aborta ANTES de pago.create -> pago.create -> re-lectura del saldo vía tx (no stale) -> flip a PAGADA si llega a 0. Reviewer confirmó que los mocks de test fuerzan el uso real de tx (no tenantDb directo) -- una llamada al objeto equivocado habría hecho fallar los tests, no solo coincidir superficialmente con el brief. 6/6 tests, tsc clean.

Task 7: complete (commit 2f0a95d, pushed to main, review APPROVED first pass -- verbatim match, no fixes needed). /facturas/[id] detail page (subtotal/descuento/iva/total/saldoPendiente vía .toString(), mismo patrón de Decimal que el resto de la app) + RegistrarPagoForm (solo recibe facturaId, sin leak de campos sensibles; solo se renderiza si estado===PENDIENTE, sin flujo de reversión). 236/236 tests (1 timeout preexistente no relacionado en tenant-client.test.ts, confirmado fuera del alcance del diff), tsc clean.

Task 8: complete (commits 66604d4 + ccc960e, pushed to main, review APPROVED after one fix loop). e2e/tenant-flow.spec.ts extendido: TERMINADA -> Generar factura con descuento -> verificar subtotal/IVA/total -> pago parcial -> pago final -> PAGADA -> stock decrementado 20->18 (estable a través de ENTREGADA). REAL BUG encontrado y corregido por el controller (no por el implementer del task, que quedó NEEDS_CONTEXT): GenerarFacturaForm (Task 5) usaba useActionState + useEffect para navegar tras éxito, pero crearFacturaAction's revalidatePath(`/ordenes/${ordenId}`) hace que la página padre re-renderice y reemplace el form por el link "Ver factura" en cuanto orden.factura se puebla -- desmontando el form ANTES de que su propio efecto pudiera disparar router.push. Diagnosticado con logging real contra un dev server corriendo en vivo (no adivinado): primer intento de fix usó redirect() server-side dentro de la action, verificado y revertido porque en la arquitectura de subdominio-por-tenant de este proyecto el redirect() perdía el subdominio (el GET de seguimiento llegó a host localhost:3000 en vez de taller-e2e-smoke.localhost:3000, disparando el guard de tenant-mismatch -- el guard funcionó correctamente, el bug era la pérdida de host). Fix final: GenerarFacturaForm reescrito con useTransition + handleSubmit manual, router.push() se dispara sincrónicamente dentro del mismo callback que llama a la action, antes de que React procese cualquier RSC update de revalidatePath -- única desviación del patrón useActionState del resto del código, justificada y documentada inline. Reviewer verificó independientemente la matemática de dinero/stock del e2e contra el código real de Tasks 2/3, sin regresión en el resto de forms (RegistrarPagoForm no necesitaba el mismo fix, no navega). 239/239 unit tests, 2/2 e2e, tsc clean.

======================================================================
FASE 4 (FACTURACION Y PAGOS): ALL 8 TASKS COMPLETE (2026-08-20)
======================================================================
All 8 tasks implemented via subagent-driven-development, task-scoped-reviewed (1 real fix loop: Task 8's navigation race, diagnosed and fixed by the controller directly against a live dev server after the task's own implementer got stuck). 239/239 unit tests + 2/2 e2e passing, tsc clean.

## Final whole-branch review (2026-08-20)
Dispatched final review (opus) over the full Fase 4 diff (56bf295..0548b84, commits 4667778..0548b84). Found 2 Critical + 2 Important (a 5th, cosmetic finding folded into one of the Critical fixes):
1. CRITICAL (C1) - `assertOrdenMutable` only checked `orden.estado`, not whether the orden already had a `Factura`. `TERMINADA` was the only estado simultaneously facturable and mutable -- items/mano-de-obra/DVI could still be added or deleted on an already-invoiced orden, letting the factura's stored totals silently diverge from the orden's live state with no stock reconciliation.
2. CRITICAL (C2) - the stock decrement inside `crearFacturaAction`'s `$transaction` was an unguarded plain `update` (no floor check) -- `stockActual` could go negative.
3. Important (I1) - `RegistrarPagoForm` was conditionally mounted/unmounted by the parent Server Component based on `estado`, racing the final balance-zeroing payment's own confirmation -- same bug class as Task 8's navigation race.
4. Important (I2) - a 100%-discount or zero-line-item factura was created as an unclosable `PENDIENTE` with `saldoPendiente: 0` instead of `PAGADA`.

FIXED in commit d515b52 (pushed to main):
- C1: `assertOrdenMutable` (src/lib/orden/mutable-guard.ts) now also rejects when `orden.factura` is truthy. All three consumers (item-orden-actions.ts, mano-de-obra-actions.ts, and dvi-actions.ts -- a third consumer the implementer identified themselves, not originally called out, needed the same signature widening to keep tsc clean) updated to `select: { estado: true, factura: { select: { id: true } } }`.
- C2: stock decrement now an atomic `updateMany` + `count === 0` guard (mirrors `registrarPagoAction`'s existing overpayment guard), inside the same `$transaction` callback so a triggered guard rolls back the whole transaction (including the already-created factura row).
- I1: `RegistrarPagoForm` now always mounted, branches PENDIENTE/PAGADA internally.
- I2: `estado: total <= 0 ? "PAGADA" : "PENDIENTE"`.
- Folded in: duplicate-factura pre-check's orden lookup narrowed from `factura: true` to `factura: { select: { id: true } }` (only truthiness needed, avoids a Decimal-bearing full-row fetch).
- 250/250 unit tests, 2/2 e2e, tsc clean (implementer's own verification run).

Independently re-reviewed (opus, adversarial -- re-traced all four fixes against the actual call sites rather than trusting the implementer's report, ran tsc/vitest itself instead of the reported numbers). Verdict: all 4 findings Resolved, no regressions to legitimate non-invoiced mutation flows. Re-review's own tsc run clean; own vitest run 249/250 (1 failure: `scripts/provision-tenant.test.ts`'s live-Postgres integration test timed out at 20s -- confirmed pre-existing/unrelated, touches no file in d515b52's diff, not retried per RULES.md #1). e2e not re-run by the reviewer (requires a live Postgres-backed dev server, RULES.md #2 forbids waiting on it) -- implementer's 2/2 Playwright claim stands unverified by the re-review, not contradicted.

**Ready to merge: Yes.**

New Minor/backlog items surfaced by the re-review (not fixed, deferred per RULES.md #7):
- `ordenes/[id]/page.tsx` hides `AgregarItemForm`/`AgregarManoObraForm` behind `!orden.factura` but still renders `DviChecklistForm`/`DviFotoForm` unconditionally, even though `dvi-actions.ts` now rejects them post-factura too -- guard holds (submit fails with the correct error), but the form is visibly present when it shouldn't be. Narrow window: TERMINADA + factura already generated (ENTREGADA was already fully immutable before this fix).
- This session's original Fase-4-review "I3" finding has no recorded text anywhere in the repo (fix report explicitly scoped it out; no other artifact captured it) -- a documentation gap in this ledger, not a known code defect. If it resurfaces, treat as unknown/needs re-review rather than assuming it was fixed.
- `provision-tenant.test.ts`'s live-Postgres schema-creation test has a recurring 20s-timeout flake under load (also seen in earlier phases) -- candidate for a hookTimeout bump, not investigated further this pass.

======================================================================
FASE 4 (FACTURACION Y PAGOS): COMPLETE, REVIEWED, READY TO MERGE (2026-08-21)
======================================================================
All 8 tasks + 1 review-driven fix round (2 Critical + 2 Important) implemented, reviewed, independently re-reviewed, and pushed to main (final commit d515b52). Final state: 250/250 unit tests passing (249/250 on the re-reviewer's own independent run, 1 pre-existing unrelated flake), 2/2 e2e (per implementer, not independently re-run), tsc --noEmit clean. No branch/PR (direct-to-main per established project convention). Next: Fase 5 (Dashboard y Reportes básicos) per the design doc's roadmap.

## FASE 4 SUMMARY
- 8 tasks completed (final feature commit ccc960e/0548b84) + 1 review-fix commit (d515b52)
- 250/250 unit tests passing, 2/2 e2e, tsc clean
- 2 Critical + 2 Important findings from the final whole-branch review, all fixed and independently re-verified: post-factura mutability gap (items/mano-de-obra/DVI could still change after invoicing), unguarded stock decrement (could go negative), payment-form unmount race, unclosable zero-total factura
- Technical debt documented: DVI form visibility inconsistency post-factura (Minor), missing original-I3 finding text (documentation gap), provision-tenant.test.ts timeout flake, plus all backlog carried from Fases 1-3 (see their sections above)
- Status: Fase 4 complete, ready for Fase 5

======================================================================
# TorqueFlow Fase 5 (Dashboard y Reportes básicos) -- Progress Ledger
======================================================================
Started 2026-08-21. Plan: docs/superpowers/plans/2026-08-21-torqueflow-phase5-dashboard.md (11 tasks). Executing via subagent-driven-development directly on main (explicit standing user consent, same as Fases 1-4 -- no worktree). Read-only phase over the Fases 2-4 schema, no migration, no new npm dependency. Reportes/dashboard actions requireRole(["ADMIN"]) only, deliberately -- rentabilidad exposes cost/margin, productividad exposes peer-comparison performance data.

Task 1: complete (commits 2da468c..7534d26, review clean). roundMoney extracted from src/lib/factura/totales.ts into shared src/lib/money/round.ts, behavior-preserving (existing computeFacturaTotales tests unaffected). 253/253 tests.

Task 2: complete (commits 7534d26..4bc74fa, review clean). src/lib/reportes/rango-fechas.ts: buildRangoFechas (half-open {gte,lt} UTC interval, hasta inclusive) + rangoMesActual (default current-month-to-date range). Reviewer confirmed boundary arithmetic is DST-immune epoch-ms addition, leap-year and year-rollover cases correctly tested. 259/259 tests.

Task 3: complete (commits 4bc74fa..04f84b2 + fix 4f5090c, review clean after one fix loop). src/lib/validation/reporte.ts: reporteFiltrosSchema (mandatory desde/hasta, optional sedeId, three exact error strings later tasks assert verbatim). Fix loop closed a real Important finding: the cross-field desde<=hasta .refine() ran unconditionally even when a date already failed its own fechaSchema validation (Zod "dirty" status doesn't abort the parent), producing a spurious second error message for malformed-but-lexicographically-ordered inputs. Restructured with superRefine gated on both dates independently passing fechaSchema first; new regression test asserts exactly one issue for the repro case. Re-reviewer independently re-derived the Zod ParseStatus mechanics (not just trusted the test). 268/268 tests, tsc clean.

Task 4: complete (commits 2aa68b6..2db3a88, review clean first pass). src/lib/reportes/rentabilidad.ts: computeRentabilidad (facturasCount, totalFacturado, costoRepuestos, margen, margenPorcentaje, manoDeObraFacturada). Reviewer independently hand-computed all 5 test cases (single/multi factura, negative margin, rounding boundary) and confirmed the highest-risk property -- manoDeObraFacturada never mixed into margen/margenPorcentaje -- holds. 273/273 tests.

Task 5: complete (commits 2db3a88..e48f3aa, review clean first pass). src/lib/reportes/productividad.ts: computeProductividad groups ENTREGADA-derived órdenes by mecanicoId, "Sin asignar" bucket for null (sentinel key verified collision-safe against real cuid() ids), sorted by montoManoDeObra desc then mecanicoNombre.localeCompare(..., "es") asc. Reviewer independently traced the sort comparator sign/tie-break and confirmed zero-manoDeObra órdenes still count. 280/280 tests.

Task 6: complete (commits e48f3aa..2915b94, review clean first pass, security-scrutinized). src/app/actions/reporte-actions.ts: getReporteRentabilidad -- requireRole(["ADMIN"]) unconditionally first (no try/catch), tenantDb from session.user.tenantSchema (no resolveTenant()), select-only through orden.items.repuesto/manoDeObra, every Decimal converted (cross-checked against schema.prisma line-by-line), resolveSedeId private helper (explicit sedeId skips lookup, no-sede returns zeroed totals without querying facturas). 287/287 tests, tsc clean.

Task 7: complete (commit 21689ca, review clean first pass, security-scrutinized). getReporteProductividad added to reporte-actions.ts, reusing Task 6's ReporteFiltros/ReporteFiltrosAplicados/resolveSedeId (not redefined), anchored on entregadaAt+estado:"ENTREGADA"+sedeId (deliberately not Factura.createdAt), select-only mecanico:{select:{nombre:true}}. Implementer caught and fixed a genuine brief typo (test literal for "2026-13-01" expected the format error but that input passes the shape regex and only fails the calendar refine) -- reviewer independently traced fechaSchema/esFechaDeCalendario and confirmed the fix is correct, only the test string changed, no production code touched to mask a bug. 293/293 tests, tsc clean.

Task 8: complete (commit 1ec529f, review clean first pass). src/app/(dashboard)/reportes/page.tsx: plain RSC page, native GET form (desde/hasta/hidden sedeId), rentabilidad section reading getReporteRentabilidad. ADMIN-conditional nav link added to layout.tsx (purely additive, confirmed). Reviewer verified "Margen bruto:" vs "Margen bruto (%):" labels are substring-safe for Task 11's e2e, and field names match Task 6's actual RentabilidadTotales contract with no drift. 293/293 tests, tsc clean, no new unit tests (established RSC-page precedent).

Task 9: complete (commit 87546f7, review clean first pass). Productividad table added to /reportes page: sequential awaits confirmed (getReporteRentabilidad then getReporteProductividad, not Promise.all -- avoids the redirect()-throw race this project has hit before), empty-state message, row key fila.mecanicoId ?? "sin-asignar". 293/293 tests, tsc clean, no new unit tests.

Task 10: complete (commit ea811da, review clean first pass). e2e/global-setup.ts seeds a second TECNICO user (E2E_TECNICO_EMAIL/PASSWORD/NOMBRE constants, ADMIN seed preserved unchanged) + e2e/tenant-flow.spec.ts selects "Tec E2E" as mecánico when creating the smoke-test orden, via the E2E_TECNICO_NOMBRE constant (not a duplicated literal). No existing assertions touched. e2e 2/2 passing.

Task 11 (final task): BLOCKED then complete (commits e796eda + 23785e8 + 7518b99, review clean after two fix loops, security-scrutinized). e2e extended through /reportes: rentabilidad numbers, productividad row, date-range round-trip via searchParams, 2020 range zeroes both reports, inverted-range Zod error, ADMIN-only role gate (TECNICO sees no nav link, direct navigation to /reportes redirects to /login?error=forbidden with exact copy).
  Blocker discovered mid-task: signing out and re-logging-in as TECNICO on the same tenant subdomain (never exercised by any prior e2e) revealed a real bug -- signOut's callbackUrl redirected to bare localhost:3000/login instead of the tenant subdomain, because local .env's AUTH_URL overrides trustHost's per-request Host-header resolution for that one redirect path. This is exactly Fase 1's previously-deferred backlog item R5 ("AUTH_URL... footgun if copied to prod"), now proven to be a real bug, not just a hypothetical one. User chose a code-level fix over editing .env.
  Fix 1 (e796eda): new src/lib/auth/resolve-redirect.ts (resolveRedirectUrl, pure + unit-tested, mirrors Fase 1 Task 25's authorize-credentials.ts extraction precedent) reuses the existing extractTenantSlug to trust only the bare BASE_DOMAIN or a valid tenant subdomain of it -- any other absolute URL falls back to NextAuth's own baseUrl. Wired into auth.ts's new redirect callback; sign-out-button.tsx now passes `${window.location.origin}/login`. Security review independently traced every URL shape (external host, protocol-relative, credentials-embedded, non-http schemes) and confirmed no path returns an attacker-controlled URL -- fails closed in every untrusted case. Approved, no Critical/Important findings.
  Fix 1 exposed a second, narrower bug: with the redirect now correct, the next e2e line (`getByRole("alert")`) started hitting a Playwright strict-mode violation -- ambiguous against Next.js's own `__next-route-announcer__` (also role="alert"). Fixed with `.filter({ hasText: ... })`, same precedent as Fase 2 Task 14's status-role fix; reviewer confirmed collision is structurally impossible (route announcer content is the static app title "TorqueFlow", never the forbidden-error text).
  Fix 2 (7518b99): review of Task 11 itself found one Important, plan-mandated gap -- the productividad "Tec E2E" row asserted horas (1.5) and monto (30) but never the ordenesCompletadas cell (1), so a miscounted-orden regression could slip through. Fixed with a precise `filaTecnico.locator("td").nth(1)).toHaveText("1")` (column index verified against the real page markup). Re-review confirmed RESOLVED, Approved.
  Final state: 299/299 unit tests, 2/2 e2e, tsc clean.

======================================================================
FASE 5 (DASHBOARD Y REPORTES BÁSICOS): ALL 11 TASKS COMPLETE, TASK-SCOPED REVIEWS ALL CLEAN (2026-08-21)
======================================================================
All 11 planned tasks + 2 mid-flight bug fixes (sign-out tenant-subdomain redirect, e2e productividad-count coverage gap) implemented, task-scoped-reviewed (4 fix loops total across Tasks 3, 7's self-caught brief typo, and Task 11's two fixes), pushed to main.

## Final whole-branch review (2026-08-21)
Dispatched final review (opus) over the full Fase 5 diff (3b1e41f..6adad8e, excluding the plan doc and progress.md). Six cross-cutting checks performed (report-action consistency, resolveRedirectUrl vs. other NextAuth flows, e2e coverage honesty, money-rounding double-application, cross-task interactions no single review could see, backward compatibility with Fases 1-4) -- all clean except one Critical:

1. CRITICAL - `margen`/`margenPorcentaje` in `computeRentabilidad` subtracted `costoRepuestos` (net, IVA-exclusive cost) from `totalFacturado` (sum of `Factura.total`, IVA-INCLUSIVE) -- overstated gross margin by the full 19% IVA amount on every invoice. On the e2e's own fixture: page showed "Margen bruto: 124.18 (88.59%)" when the correct IVA-exclusive margin was 101.80 (86.42%). This was the plan's own literal arithmetic (Task 4's worked example), not an implementer deviation -- presented to the user as a plan-mandated design decision requiring their call, per this project's review protocol for plan-mandated findings.
2. Important - `costoRepuestos` reads the CURRENT `Repuesto.precioCompra` (a Fase 3 write path `updateRepuestoAction` can edit any time, no cost snapshot on `ItemOrden`), so editing a part's catalog cost retroactively rewrites historical margin for every closed period that ever sold it. This is already an explicitly-documented, deliberately-accepted v1 limitation in the plan's own Global Constraints ("accepted v1 approximation... out of this phase's read-only scope") -- reviewer correctly re-surfaced it as a cross-cutting risk, but the plan already made this call with reasoning; no new action taken, stays documented backlog (needs a cost-snapshot column + migration in a future phase before any real production deploy with price-volatile catalogs).

User chose to fix #1 (not accept-and-document). FIXED in commit 13f7c33 (pushed to main): `RentabilidadFactura` gained a `base: number` field (`Factura.subtotal - Factura.descuento`, the tax-exclusive base), `computeRentabilidad` now accumulates `baseTotal` separately from `totalFacturado` and computes `margen`/`margenPorcentaje` from `baseTotal`, not `totalFacturado` (which keeps its original IVA-inclusive meaning, reported unchanged as "Total facturado"). `RentabilidadTotales`'s six public field names unchanged -- no breaking change to the page. `getReporteRentabilidad`'s select gained `subtotal`/`descuento` (Number-converted, same discipline as `total`). Tests and e2e updated with internally-consistent, hand-verifiable fixture numbers. Independently re-reviewed: arithmetic hand-recomputed for the e2e case (101.80/86.42%) and a second multi-factura unit test case (190/79.17%), both confirmed exactly correct, no leftover references to the old formula. 299/299 unit tests, 2/2 e2e, tsc clean.

**Ready to merge: Yes.**

Minor findings from the review NOT addressed (deferred to backlog, none blocking):
- `/reportes` page awaits the two report actions sequentially and each independently re-runs the identical default-sede lookup -- two redundant queries + a narrow window where a Sede created between the two calls could scope the tables inconsistently. `Promise.all` + a shared resolved sede would fix both.
- `productividad.error` is never rendered on the page (only `rentabilidad.error` is) -- on a validation error the productividad section still shows "No hay órdenes entregadas..." instead of the real error.
- `BASE_DOMAIN ?? "zdevs.uk"` default duplicated in both `auth.ts` and `middleware.ts` with no shared constant -- only middleware fails fast if unset in production; low risk today, will drift if either default ever changes without updating the other.
- `round.test.ts`'s test name ("rounds negative values away from zero") contradicts its own correct assertion (JS actually rounds toward +Infinity at the exact half) -- misleading name, not a code defect.
- The `sedeId` GET-form round-trip (hidden input + explicit-sede path in both actions) is claimed "end to end" in a page comment but is only unit-tested, never exercised by e2e (no e2e ever supplies an explicit sedeId).
- `resolveRedirectUrl` preserves the incoming URL's scheme verbatim on the trusted-host branch (doesn't force https) -- low impact behind Cloudflare, but worth noting since this function is now the app's redirect-trust authority.

======================================================================
FASE 5 (DASHBOARD Y REPORTES BÁSICOS): COMPLETE, REVIEWED, READY TO MERGE (2026-08-21)
======================================================================
All 11 tasks + 3 fix rounds (Task 3's date-ordering short-circuit, Task 11's sign-out-redirect + locator + productividad-count fixes, and the final review's margin-IVA fix) implemented, reviewed, independently re-reviewed, and pushed to main. Final state: 299/299 unit tests passing, 2/2 e2e, tsc --noEmit clean. No branch/PR (direct-to-main per established project convention). Next: Fase 6 (Gestión de Sedes) per the design doc's roadmap.

## FASE 5 SUMMARY
- 11 tasks completed (final task-11 commit 7518b99) + 3 fix commits (Task 3 date-ordering, Task 11's sign-out redirect e796eda + locator/count fixes, final-review margin-IVA 13f7c33)
- 299/299 unit tests passing, 2/2 e2e, tsc clean
- Read-only phase, no migration, no new npm dependency -- pure aggregation over the existing Fases 2-4 schema
- 1 Critical (margen mixing IVA-inclusive revenue with net cost) + 1 Important (catalog cost mutability, already an accepted documented v1 limitation) found by the final whole-branch review; Critical fixed and independently re-verified, Important confirmed as already-deliberate plan scope
- A real, previously-hypothetical Fase 1 backlog item (R5: local .env's AUTH_URL overriding trustHost) manifested as an actual bug during Task 11's e2e (first path ever to exercise sign-out+re-login) -- fixed with a pure, unit-tested, security-reviewed `resolveRedirectUrl` that trusts only same-BASE_DOMAIN subdomains, never an arbitrary host
- Technical debt documented: sequential/redundant sede lookups on the reportes page (Minor), productividad.error not rendered (Minor), BASE_DOMAIN default duplication (Minor), unexercised sedeId e2e path (Minor), catalog-cost snapshot still missing (Important, deferred with the plan's own reasoning), plus all backlog carried from Fases 1-4
- Status: Fase 5 complete, ready for Fase 6

======================================================================

# TorqueFlow Fase 7 (Agendamiento de citas + recordatorios de mantenimiento) -- Progress Ledger

Plan: docs/superpowers/plans/2026-08-21-torqueflow-phase7-citas-recordatorios.md

Task 1: complete (commit 1e72ba4). Cita/EstadoCita, ConfiguracionSmtp (singleton con CHECK id='singleton'), RecordatorioEnviado/MotivoRecordatorio + migración 20260821210000. `@@map("configuracion_smtp")` es singular (excepción deliberada plan-mandated a la convención "tabla_en_plural", aceptada dado que el modelo es un singleton real forzado por la base de datos). 13/13 tests.

Task 2: complete (commit 6c3ae5a). scopeCita(sedeActivaId) en src/lib/sede/scope.ts, Prisma-type-free como sus hermanas. 8/8 tests.

Task 3: complete (commit 47e217e). src/lib/crypto/secret-box.ts (AES-256-GCM, envelope versionado "v1:iv:tag:cipher", SMTP_ENCRYPTION_KEY sin fallback) + src/lib/email/smtp-config.ts (CONFIGURACION_SMTP_ID, descifrarConfiguracionSmtp que nunca expone passwordCifrado/activo). 14/14 tests.

Task 4: complete (commit a311582). src/lib/validation/cita.ts + smtp.ts, Zod schemas con mensajes en español pineados por test. 15/15 tests.

Task 5: complete (commit e9c08ff). src/app/actions/cita-actions.ts, sede-scoped e IDOR-safe (findFirst+scopeCita nunca findUnique; updateMany/deleteMany+scopeCita+count===0 nunca update/delete por id crudo), roles ADMIN+RECEPCION para crear/editar/cambiar estado, ADMIN-only para borrar. 17/17 tests.

Task 6: complete (commit 22897a0, ejecutada después de Task 10 por dependencia). src/app/actions/smtp-actions.ts, ADMIN-only, password nunca expuesto (ni cifrado ni plano) en ningún valor de retorno, contraseña vacía en el form = "conservar la existente". 13/13 tests.

Task 7: complete (commit c11c905, INLINE -- classifier bloqueó el despacho del implementador). UI de citas (lista, formulario de reserva, detalle con notFound() real como boundary IDOR, formulario de cambio de estado) + link de nav. 5/5 tests nuevos.

Task 8: complete (commit 8679889). UI de configuración SMTP, ADMIN-only, campo de contraseña con defaultValue="" literal (nunca condicionado a un valor del servidor). 6/6 tests.

Task 9: complete (commit 4839519, ejecutada antes de Tasks 6/10 por dependencia). src/lib/recordatorios/mantenimiento.ts, regla pura "5.000 km / 6 meses, lo que ocurra primero" + cooldown de 90 días, cero imports de Prisma. 16/16 tests.

Task 10: complete (commit 2aa2aa8, INLINE -- classifier bloqueó el despacho del implementador). src/lib/email/enviar-email.ts (único módulo consciente de Nodemailer) + src/lib/recordatorios/plantilla.ts (texto plano + HTML básico con escape de todo dato de cliente). `npm install nodemailer` resolvió 8.0.11 (vulnerabilidad alta GHSA-p6gq-j5cr-w38f vía la opción `raw`, no usada en este código) porque @auth/core fija nodemailer a `^7.0.7 || ^8.0.5` -- backlog: actualizar a 9.x cuando @auth/core lo permita. 10/10 tests.

Task 11: complete (commit cfcbd9d). src/lib/recordatorios/ejecutar-recordatorios.ts (orquestador DB-free, tolerante a fallos parciales por tenant y por vehículo, registrarRecordatorio solo tras un envío exitoso) + src/lib/recordatorios/gateway-prisma.ts (único módulo Prisma-aware del recordatorio). 13/13 tests.

Task 12: complete (commit 0ee4087). GET /api/cron/recordatorios, sin sesión (llamador externo), auth por CRON_SECRET vía Authorization Bearer comparado con timingSafeEqual sobre digests SHA-256, fail-closed si CRON_SECRET no está configurado. 9/9 tests.

Task 13: complete (commit beedea8, dos rondas de despacho -- la primera se detuvo correctamente BLOCKED tras corregir dos bugs reales del código literal del brief y diagnosticar una tercera causa raíz sin aplicarla; la segunda aplicó esa corrección diagnosticada). e2e extendido: RECEPCION agenda+confirma una cita en Sede principal; el boundary IDOR se prueba por URL directa con status HTTP 404 real (no solo ocultamiento en lista); /configuracion-smtp probado ADMIN-only tanto por ausencia del link de nav como por redirección directa de URL. playwright.config.ts timeout 30s->90s (fuera de los archivos declarados en el brief, necesario porque el test e2e compartido ya recorre ~25 rutas). 2/2 e2e.

## Final whole-branch review (2026-08-24)
Dispatched final review (opus) over the full Fase 7 diff (00b2347..bc1a5bd). Layering/security posture confirmed excellent under composition, not just per-file: secret handling airtight (traced every path a decrypted password could reach, none leak), cron auth confirmed genuinely fail-closed with middleware.ts confirmed inert on that path, cita sede scoping byte-consistent with Fase 6 (no findUnique/update/delete-by-id anywhere), maintenance-due rule confirmed correct on every edge case by hand (divide-by-zero, negative rates, DST-proof UTC arithmetic). 0 Critical. 5 Important:
1. No scheduler wired to GET /api/cron/recordatorios (plan gap, not implementation deviation) -- deferred to user, who will configure system cron on their own Ubuntu/Docker deployment; not blocking, no code change needed.
2. The ledger's "one duplicate at most" claim for a registrarRecordatorio failure after a successful send did not actually hold for a deterministic (non-transient) write failure -- would have re-sent forever, unbounded.
3. listarVehiculosParaRecordatorio had no `where` clause at all -- fetched every vehicle in the tenant, risking a timeout at real scale.
4. Zero logging anywhere in the feature -- a production sweep failure was completely invisible (cron route's catch discarded the real error, orchestrator's catches only fed the returned summary).
5. `citaInputSchema`'s datetime-local parse resolved against the SERVER's local timezone, not the workshop's -- invisible today (render uses the same server zone) but silently wrong the moment anything timezone-aware (Fase 8 emails, a redeploy to a different-zone host) touches the stored instant.

FIXED in commit 5d3fb47 (pushed to main), 4 of 5 Important addressed per explicit user decision (#1 deferred, user-owned deployment concern):
- #2: registrarRecordatorio now retried once in-process on failure; if the retry also fails, the vehicle is counted in enviados AND a new enviadosNoRegistrados field (not fallidos, since the email genuinely sent), with a distinctly RIESGO_DUPLICADO-prefixed error string -- makes the residual risk visible instead of silently unbounded. Controller independently traced the control flow (mensajeEnviado flag, retry placement, outer-catch branching) against the diff, not just the report.
- #3: gateway-prisma.ts gained a coarse, necessary-not-sufficient SQL pre-filter (must have a delivered orden; must not have a RecordatorioEnviado newer than the 90-day cooldown) -- deliberately no pagination added (explicit user scope decision), no attempt to replicate the km/6-month "whichever first" logic in SQL.
- #4: a single registrarError chokepoint in ejecutar-recordatorios.ts now console.error's the same pre-sanitized (constructor-name-only) string already pushed to resumen.errores; route.ts's catch logs the same safe categorization inline (not imported, to avoid breaking route.test.ts's wholesale module mock -- documented, reasonable interpretive call). Verified: never logs a raw Error object or .message anywhere, consistent with describirError's existing security posture.
- #5: fixed OFFSET_TALLER = "-05:00" (Colombia has no DST) appended in citaInputSchema's transform; both citas page.tsx/[id]/page.tsx's Intl.DateTimeFormat gained explicit timeZone: "America/Bogota" so rendering doesn't depend on the server's location either.
- Controller independently re-ran all 3 directly-modified test files (33/33) and tsc (clean) rather than trusting the fix report alone.
9 Minor findings from the review NOT addressed (deferred to backlog, none blocking): updateCitaAction/deleteCitaAction have no UI caller (plan's own Task 7 scope only covered create+edit-estado+detail); no boot-time validation of SMTP_ENCRYPTION_KEY (only fails lazily on first save, unlike BASE_DOMAIN's eager middleware check); the accepted RIESGO_DUPLICADO path itself has no dedicated "double-failure" test beyond what commit 5d3fb47 already added; cifrarSecreto can throw outside the try in guardarConfiguracionSmtpAction (crashes the Server Action instead of a form message, no secret leak); fallidos conflates whole-tenant failures with single-vehicle failures under one counter; missing composite (sede_id, fecha_hora) index on citas (two separate indexes exist instead); errores[] carries placas/schema names over HTTP (acceptable -- reader holds CRON_SECRET, a platform operator); z.literal("") after .optional() is a no-op in cita.ts/smtp.ts (cosmetic); no .trim() on SMTP host/usuario/fromEmail.

## FASE 7 SUMMARY
- 14 tareas completadas + 1 review-fix commit (5d3fb47, 4 of 5 Important findings fixed, 1 deferred by explicit user decision)
- 511/511 unit tests pasando antes del fix (3 skipped) aislando el flake de contención de esquema en paralelo ya documentado; tras el fix, 506/507 en la corrida completa con el mismo tipo de flake (esta vez en provision-tenant.test.ts) confirmado 13/13 limpio en aislamiento; 2/2 e2e, tsc clean
- Decisiones de arquitectura tomadas en esta fase (vinculantes para Fase 8):
  - ConfiguracionSmtp es una fila singleton por schema de tenant (id literal 'singleton' + CHECK en la migración), no una tabla de filas.
  - La contraseña SMTP se guarda cifrada con AES-256-GCM (src/lib/crypto/secret-box.ts, sobre "v1:iv:tag:cipher") bajo SMTP_ENCRYPTION_KEY en .env. Rotar esa clave invalida todas las contraseñas guardadas.
  - La de-duplicación de recordatorios es la tabla RecordatorioEnviado con un cooldown fijo de 90 días, no un campo en Vehiculo.
  - El endpoint de cron (GET /api/cron/recordatorios) se autentica con CRON_SECRET en "Authorization: Bearer", comparado con timingSafeEqual sobre digests SHA-256; sin CRON_SECRET responde 401 a todos (fail closed).
  - El reloj de 6 meses lee OrdenTrabajo ENTREGADA/entregadaAt, NO HistorialVehiculo (texto libre, sin tipo de servicio, fecha por defecto now()).
  - El umbral de 5.000 km es una proyección km/día derivada de las dos lecturas de kilometrajeIngreso más recientes; con menos de dos lecturas solo aplica el umbral de tiempo.
  - Los nuevos action files llaman al guard ANTES de validar el FormData (mejora deliberada sobre la convención de Fases 2-4, que valida primero).
- Fase 8 hereda la infraestructura SMTP completa (config por tenant, cifrado, transporte Nodemailer, plantillas texto+HTML básico) y solo debe añadir las notificaciones de estado de orden (módulo 6 §5).
- Explícitamente fuera de alcance en esta fase: página pública de reservas, WhatsApp/SMS, proveedores externos de email, motores de plantillas, Plan/maxSedes (Fase 9).
- Deuda técnica documentada:
  - nodemailer 8.0.11 en el árbol de dependencias (GHSA-p6gq-j5cr-w38f, alta severidad) -- el vector de explotación (opción `raw`) no se usa en este código, pero @auth/core bloquea la actualización a 9.0.5 hasta que soporte ese rango de peer dependency
  - configuracion-smtp-form.tsx usa la verdad de `configuracion` en lugar del booleano más preciso `passwordConfigurada` para el hint de "conservar contraseña" (equivalente hoy, campo sin usar)
  - probarConfiguracionSmtpAction traga el error real en un catch vacío (deliberado para no filtrar detalles SMTP, pero también oculta bugs genuinos de descifrado)
  - si registrarRecordatorio falla justo después de un envío exitoso, el vehículo se reintenta en el siguiente barrido (riesgo acotado de email duplicado, trade-off documentado a favor de "nunca silenciar" sobre "nunca duplicar")
  - más todo el backlog acumulado de Fases 1-6
- Status: Fase 7 complete, ready for Fase 8

======================================================================
# Decisiones de diseño diferidas (para fases futuras)
======================================================================

## Fase 8 (Notificaciones automáticas al cliente, módulo 6 §5) -- alcance acotado a email/SMTP (decidido 2026-08-21)

**Fase 8 = SOLO notificaciones por email vía SMTP del propio cliente (el taller), no WhatsApp/SMS, no proveedor externo de email.**

- Motivo: evitar costos de servicios externos (SendGrid, AWS SES, Resend, etc.) y de mensajería (Meta/Twilio/360dialog) antes de que el negocio los necesite.
- Cada tenant configura su propio servidor SMTP (host, puerto, usuario, password, fromEmail, fromName) usando el correo corporativo que ya tiene -- no un proveedor centralizado gestionado por el SaaS.
- Librería: Nodemailer (estándar de Node.js, sin costo). Password SMTP encriptado con `crypto` usando una clave maestra en `.env` (nunca en texto plano en la base de datos).
- Plantillas: texto plano o HTML básico en v1 -- no un motor de plantillas complejo.
- **WhatsApp/SMS quedan diferidos a una fase posterior** (candidata: Fase 10), condicionado a: cuenta de Meta Business verificada, presupuesto definido para costo por mensaje, templates aprobados por Meta, y una decisión de intermediario (Twilio, 360dialog) vs. API directa de WhatsApp Business.
- **Explícitamente fuera de Fase 8**: WhatsApp/SMS, SendGrid/AWS SES/Resend (o cualquier proveedor externo de email), motores de plantillas HTML complejos.

Cuando se planifique Fase 8 (después de Fase 7, agendamiento de citas), usar esta sección como el alcance vinculante en lugar de re-derivarlo del design doc, que aún no refleja esta decisión de reducción de alcance.

**ACTUALIZACIÓN (2026-08-21, al planificar Fase 7):** el usuario decidió adelantar el envío real de recordatorios por email a Fase 7, en lugar de dejarlo pendiente para esta Fase 8. Fase 7 construye ahora la infraestructura SMTP/Nodemailer descrita arriba (config SMTP por tenant, password encriptado con `crypto` + clave maestra en `.env`, plantillas de texto plano/HTML básico) para enviar los recordatorios de mantenimiento preventivo. El resto de esta sección (alcance de Fase 8, exclusiones de WhatsApp/SMS) sigue vigente sin cambios: Fase 8 pasa a ser "notificaciones automáticas al cliente sobre el estado de la orden" (módulo 6 §5) **reutilizando** la infraestructura SMTP ya construida en Fase 7, en vez de construirla desde cero.

**CONFIRMADO (al cerrar Fase 7):** la infraestructura SMTP descrita arriba ya existe y está en producción -- ver la sección "TorqueFlow Fase 7" de este mismo ledger para las decisiones vinculantes (singleton ConfiguracionSmtp, sobre AES-256-GCM, cooldown de 90 días, endpoint de cron con CRON_SECRET). Fase 8 debe reutilizarla, no reconstruirla.

======================================================================
# TorqueFlow Fase 6 (Gestión de Sedes) -- Progress Ledger
======================================================================
Started 2026-08-21. Plan: docs/superpowers/plans/2026-08-21-torqueflow-phase6-sedes.md (20 tasks). Executing via subagent-driven-development directly on main (explicit standing user consent, same as Fases 1-5 -- no worktree). User explicitly deferred Plan/maxSedes billing-tier enforcement to a future Fase 9. Agent-dispatch classifier blocked a reviewer subagent twice this phase already (same recurring issue as Fases 2-3) -- one retry each time resolved it, no need to switch to inline execution yet.

Task 1: complete (commits 532a789..6bffdef + fix c185ad8, review clean after one fix loop). UsuarioSede bridge table (composite PK usuarioId+sedeId, Usuario.sedes onDelete:Cascade, Sede.usuarios onDelete:Restrict) + backfill migration granting every existing Usuario the tenant's oldest Sede. Reviewer independently verified the backfill SQL character-by-character (ORDER BY created_at ASC LIMIT 1 + CROSS JOIN, structurally cannot leak across tenants given schema-per-tenant isolation) and the cascade directions against the generated migration.sql, not just the Prisma source. Two Important findings: (1) zero-sede-tenant edge case -- confirmed NOT applicable, provisionTenant's atomic tenant+sede creation (hardened since Fase 2 Task 1) structurally guarantees no tenant exists without >=1 Sede, and no real production tenants exist yet; (2) no committed regression test exercised the backfill logic against pre-existing data (only a manual, deleted proof) -- fixed with a real test that holds out this migration, seeds pre-existing Usuario/Sede rows with distinct createdAt, then applies the real migration and asserts the older sede was granted. Re-review confirmed RESOLVED, precise assertion (specific sede id, not a weak "some sede" check), no test-isolation/contention risk. 301/301 tests, tsc clean.

Task 2: complete (commit d4a5f5b, review clean first pass). scripts/seed-tenant-user.ts: seedTenantUser now grants every newly-seeded user a UsuarioSede row for the tenant's oldest sede via upsert (idempotent, safe on re-seed), closing the other half of Task 1's gap (existing users backfilled by migration; new users grant themselves going forward). Session interruption note: this task's two tests were written by a prior session that hit a token limit before implementing the source change -- resumed cleanly in a fresh session (confirmed RED against the unmodified source first, kept the pre-written tests as-is, implemented GREEN). 303/303 tests, tsc clean.

Task 3: complete (commit bda418f, review clean first pass -- controller reviewed directly after the Agent-dispatch classifier blocked a reviewer subagent twice in a row, same recurring issue noted at phase start). src/lib/sede/scope.ts: five pure, Prisma-type-free functions (scopeOrden, scopeBodega, scopeRepuesto, scopeEntrada, scopeFactura) returning where-fragments, the single source of truth Tasks 13-18 will import instead of hand-writing filters. Controller independently verified all five relation paths against the real schema.prisma: direct sedeId for OrdenTrabajo/Bodega, nested bodega:{sedeId} for Repuesto/EntradaMercancia (both reach sede only via their required Bodega), nested orden:{sedeId} for Factura (has no sede_id column, only reachable via its required OrdenTrabajo) -- byte-exact match, no drift. 309/309 tests (a transient 4-failure flake on the implementer's own first full-suite run was independently reproduced-clean by the controller's separate run -- confirmed pre-existing test-parallelism flakiness, unrelated to this change).

**Switched to INLINE execution starting Task 4**: the Agent-dispatch classifier began blocking BOTH implementer and reviewer subagent dispatches consistently (2 retries each, no longer resolving on retry as it had for Tasks 1-3) -- same recurring issue as Fase 3. User approved switching to inline execution for the remainder of Fase 6, same resolution as Fase 3.

Task 4: complete (commit 636724c, inline execution, self-reviewed). src/lib/auth/sede-access.ts: resolveSedeActiva(tenantDb, usuarioId, role, sedeId) -- ADMIN bypasses the UsuarioSede assignment check entirely (design doc's own rule, "el admin del taller ve todas") but NOT the sede-exists check (a nonexistent sedeId is rejected for every role, including ADMIN, before the role branch is even reached); TECNICO/RECEPCION require an explicit UsuarioSede row. Returns null uniformly for every failure mode (empty input, unknown sede, no assignment) so callers can't distinguish/leak which case occurred. 6/6 new tests, 315/315 full suite, tsc clean.

Task 5: complete (commit 7cf5f13, inline execution, self-reviewed). sedeActivaId/sedeActivaNombre added to the NextAuth type augmentation (User/Session/JWT) and threaded through auth.ts's jwt/session callbacks, mirroring how tenantSchema already flows; sedeId declared as a third Credentials field. Fixed a real bug in the brief's own test code: a top-level `const capturedConfig = {current: null}` declared AFTER `vi.mock(...)` calls but BEFORE `import "./auth"` hit a TDZ ReferenceError, because ES modules execute all static imports before the importing module's own top-level statements regardless of textual position -- the mocked next-auth factory (which writes to capturedConfig) ran before the const initialized. Fixed with vi.hoisted(), Vitest's documented pattern for exactly this capture-inside-a-mock-factory scenario. tsc intentionally NOT clean after this task alone (AuthorizedUser doesn't yet return the two new fields) -- anticipated and explicitly authorized by the brief itself ("do not weaken the types: go straight to Task 6"), resolved one task later. 4/4 new tests.

Task 6: complete (commit 3547639, inline execution, self-reviewed). authorizeCredentials now resolves sedeActivaId/sedeActivaNombre via Task 4's resolveSedeActiva, gated strictly after credential verification (a wrong password never triggers a sede lookup, closing a probing vector). AuthorizedUser gains the two fields, satisfying Task 5's User type -- tsc clean again. Found and fixed a second real gap: a pre-existing test ("wrong password") called authorizeCredentials without a sedeId, which the brief didn't update even though the new implementation now short-circuits on missing sedeId before ever reaching tenant/credential resolution -- added sedeId to that test's call rather than weakening the implementation. 322/322 full suite, tsc clean.

Task 7: complete (commit 4d64e3e, inline execution, self-reviewed). src/lib/sede/login-sedes.ts: listSedesDelTenant() -- the one sede read that runs without a session, resolving the tenant purely from the subdomain (Fase 1 Edge middleware), select-only. LoginForm gains a required Sede <select> (defaults to the first sede), submits sedeId to signIn alongside email/password, shows one uniform error for every failure mode (wrong credentials or unassigned sede alike), and disables submission with an explanatory message if the tenant has zero sedes. page.tsx feeds listSedesDelTenant() into the form; page.test.tsx mocks it so pre-existing error-message tests don't hit the database. 326/326 full suite, tsc clean.

Task 8: complete (commit 83093fe, inline execution, self-reviewed). src/lib/validation/sede.ts: sedeInputSchema (nombre required, direccion optional tolerating "", mirrors proveedorInputSchema's shape) + usuarioSedesInputSchema (sedeIds array, min 1 -- an empty selection would silently lock a TECNICO/RECEPCION out of the login sede gate entirely). Exact error strings "El nombre es obligatorio"/"Selecciona al menos una sede" pinned by test, asserted verbatim by Tasks 9/11/20. 333/333 full suite, tsc clean.

Task 9: complete (commit 0a1f061, inline execution, self-reviewed). src/app/actions/sede-actions.ts: listSedes/getSede/createSedeAction/updateSedeAction/deleteSedeAction, all requireRole(["ADMIN"]) -- deliberately stricter than bodega/proveedor CRUD's ["ADMIN","RECEPCION"], matching Fase 5's "structurally sensitive => ADMIN-only" precedent. deleteSedeAction pre-checks four refusal reasons in order (not found, last remaining sede, órdenes/bodegas attached, usuarios assigned) instead of surfacing a raw FK error, since "last sede" isn't even a foreign key -- OrdenTrabajo/Bodega both require a non-null sedeId. Brief's own test-count comment said 13, actual transcribed suite is 12 (harmless brief miscount, not a code issue) -- all 12 pass. 345/345 full suite, tsc clean.

Task 10: complete (commit bb787ea, inline execution, self-reviewed). /sedes RSC page (no component test, established precedent) + NuevaSedeForm/EditarSedeForm client forms. Per-sede label suffixes ("Nombre de Sede norte") on the edit form avoid the getByLabelText ambiguity class Fase 2 Task 14 and Fase 5 Task 11 had to fix retroactively, since EditarSedeForm renders once per row. Literals ("Sedes", "Crear sede", "Sede creada") pinned for Task 20's e2e. 347/347 full suite, tsc clean.

Task 11: complete (commit 0e45fc2, inline execution, self-reviewed). src/app/actions/usuario-actions.ts: listUsuariosConSedes (ADMIN-only, select-only, passwordHash deliberately absent) + setUsuarioSedesAction (delete-all-then-createMany in one $transaction, honest checkbox-group semantics -- whatever is checked at submit is the complete truth afterwards). Every submitted sedeId verified to exist in-tenant before writing, avoiding an opaque FK error. Explicit scope decision documented: this is a read-only user directory with sede-assignment controls, NOT user CRUD (creating/editing/deleting users and role changes are módulo 10, not assigned to this phase) -- deliberately not expanded. Noted for the record: changing a user's assignments doesn't affect their currently-issued JWT (same pre-existing property role changes already have since Fase 1), acceptable for v1. 353/353 full suite, tsc clean.

Task 12: complete (commit a7c4182, inline execution, self-reviewed). /usuarios page (sequential listUsuariosConSedes then listSedes, avoiding the Promise.all-with-throwing-guards race Fase 5 Task 9 already ruled out) + AsignarSedesForm: one checkbox per sede, each pre-checked per the user's current sedeIds, labelled "<sede> para <usuario>" for uniqueness across rows (same per-row disambiguation as Task 10). Literals ("Usuarios", "Guardar sedes de <nombre>", "Sedes actualizadas") pinned for Task 20's e2e. Marks the end of the phase's foundational/UI tasks -- Tasks 13-18 (cross-cutting sede-scoping across Fases 2-5's action files) start next. 357/357 full suite, tsc clean.

Task 13: complete (commit 2ca1431, inline execution, self-reviewed). orden-actions.ts's five reads/writes all now scoped via scopeOrden(session.user.sedeActivaId): listOrdenes/listOrdenesByVehiculo/listTecnicos gain the sede filter (combined with, not replacing, the existing estado filter); getOrden and updateEstadoOrdenAction's lookup switch from findUnique to findFirst -- findUnique cannot carry a sede filter, so leaving it in place would have been an IDOR letting a técnico in one sede resolve another sede's orden by id. createOrdenAction's oldest-sede-lookup fallback deleted entirely, replaced by session.user.sedeActivaId directly; a dedicated test asserts sede.findFirst is never called, as a regression net against silently reinstating the fallback. Test file rewritten to thread sedeActivaId through every session mock and switch the orden-lookup mock from findUnique to findFirst throughout. 359/359 full suite, tsc clean.

Task 14: complete (commit 557e288, inline execution, self-reviewed). item-orden-actions.ts/mano-de-obra-actions.ts/dvi-actions.ts's seven orden-mutation gates (addItemOrdenAction, deleteItemOrdenAction, addManoDeObraAction, deleteManoDeObraAction, updateDviChecklistAction, addDviFotoAction, deleteDviFotoAction) all switched from an unscoped ordenTrabajo.findUnique to a sede-scoped findFirst -- without this, a técnico in Sede B could have added items/mano de obra/DVI photos to a Sede A orden simply by posting its id. addItemOrdenAction's repuesto lookup also switched to scopeRepuesto, closing the same hole for catalog-linked items whose repuesto lives in another sede's bodega. addDviFotoAction's dvi.findUnique({ where: { ordenId } }) deliberately left untouched -- Dvi.ordenId is unique and the orden it points at was already sede-checked three lines above, so it cannot reach another sede. 8 new cross-sede refusal tests (one per function, plus the repuesto-scoping case) across the three test files, all pre-existing session mocks threaded with sedeActivaId. 367/367 full suite, tsc clean.

Task 15: complete (commit 36e89fb, inline execution, self-reviewed). bodega-actions.ts: listBodegas/getBodega gain scopeBodega; createBodegaAction's oldest-sede-lookup fallback replaced by session.user.sedeActivaId directly (dedicated test asserts sede.findFirst never called); updateBodegaAction/deleteBodegaAction switched from update/delete-by-id (cross-sede write holes) to updateMany/deleteMany with the sede in the where + a count===0 check -- same shape as Fase 2's deleteItemOrdenAction {id, ordenId} guard, not a new pattern. deleteBodegaAction's count===0 check deliberately sits outside the try/catch (matching Fase 2's fix for the same class of bug: throwing NO_ENCONTRADA from inside the try would get caught and re-wrapped as a generic Prisma error message). A full-suite run hit an unrelated transient flake in scripts/seed-tenant-user.test.ts (parallel-schema contention under load, matches this project's previously-documented pattern); re-ran that file in isolation, 3/3 clean. 370/370 full suite, tsc clean.

Task 16: complete (commit 72eece1, inline execution, self-reviewed). repuesto-actions.ts + entrada-mercancia-actions.ts: all reads gain scopeRepuesto/scopeEntrada (repuestos and entradas both inherit sede only through their required Bodega, no direct sedeId column). Every write that accepts a client-supplied bodegaId (createRepuestoAction, updateRepuestoAction, createEntradaMercanciaAction) now proves that bodega belongs to the sede activa BEFORE writing, via a bodega.findFirst + scopeBodega check returning the shared "La bodega seleccionada no pertenece a tu sede activa." message -- without this, a user could post another sede's bodega id and create a repuesto/entrada over there. updateRepuestoAction/deleteRepuestoAction switched to updateMany/deleteMany + count===0 (same cross-sede-write-hole fix as Task 15's bodega actions). addEntradaItemAction's Promise.all pair switched from findUnique to sede-scoped findFirst for both entrada and repuesto lookups; the existing repuesto.bodegaId !== entrada.bodegaId cross-check left untouched (narrower, complementary invariant). 374/374 full suite, tsc clean.

Task 17: complete (commit 619236b, inline execution, self-reviewed). factura-actions.ts + pago-actions.ts: listFacturas/getFactura gain scopeFactura (Factura has no sede_id, reaches one only through its required OrdenTrabajo); crearFacturaAction's orden lookup switched to scopeOrden'd findFirst. registrarPagoAction's factura lookup switched to scopeFactura'd findFirst. Two lookups deliberately left untouched, with reasoning: crearFacturaAction's stock-decrement updateMany (only ever touches repuestoIds that came off an already-sede-checked orden, themselves sede-checked at insert time by Task 14) and registrarPagoAction's findUniqueOrThrow inside the transaction (re-reads the same already-sede-checked facturaId inside the same transaction, cannot reach another sede). 378/378 full suite, tsc clean.

Task 18: complete (commit 27fa610, inline execution, self-reviewed). reporte-actions.ts: Fase 5's private resolveSedeId (oldest-sede DB lookup) deleted entirely -- default is now session.user.sedeActivaId, no query. ReporteFiltrosAplicados.sedeId changed from string|null to string (a sede activa always exists once a session is issued, per Task 6), and both "tenant has no sede" zeroed-result branches disappear as a direct, structural consequence, not an opportunistic fix. Fase 5's own Minor finding ("each report action independently re-runs the identical default-sede lookup") evaporates the same way. /reportes page gains a real <select> (replacing Fase 5's hidden input) defaulting to whatever the actions resolved -- this is the one deliberate place an ADMIN reads across sedes on purpose (design doc's "sede como dimensión de filtro/comparación"), already gated by requireRole(["ADMIN"]) with no extra guard needed. 378/378 full suite (net zero test-count change; the fallback tests were replaced 1:1 by sede-activa-default tests), tsc clean.

Task 19: complete (commit 5aba16a, inline execution, self-reviewed). CambiarSedeButton: SignOutButton with different copy, on purpose -- v1's way to change sede is to log in again (a live JWT swap would need this phase's first SessionProvider + client session hook, deliberately out of scope, see Task 6/Global Constraints). Reuses Fase 5's resolveRedirectUrl-safe `${window.location.origin}/login` pattern exactly (same AUTH_URL footgun class, already fixed once). Dashboard header now shows "Sede: <nombre>" on every page (the sede activa scopes everything below it) and gains ADMIN-only "Sedes"/"Usuarios" nav links alongside the existing "Reportes" one -- cosmetic only, the real boundary is requireRole(["ADMIN"]) inside listSedes/listUsuariosConSedes. Full-suite run hit the same previously-documented scripts/seed-tenant-user.test.ts parallel-schema flake as Task 15; re-ran in isolation, 3/3 clean. 379/379 full suite, tsc clean.

Task 20 (final task): complete (commit 06dade4, inline execution). e2e extended with the single most important behavioural proof of the phase: a técnico logged into Sede B never sees Sede A's data. Sede selection added to both existing logins (ADMIN, TECNICO); appended the full cross-sede flow -- ADMIN creates "Sede norte", RESTRICT guard confirmed (Sede principal can't be deleted while it has órdenes/bodegas), técnico assigned to both sedes, then proven empty in Sede norte across órdenes/bodegas/repuestos/facturas while Clientes stays visible (tenant-wide by design), then proven NOT deleted/hidden by switching back to Sede principal, ADMIN's UsuarioSede bypass confirmed (never assigned to Sede norte, logs in anyway), read-only cross-sede reportes comparison confirmed (switching the report's own sede selector doesn't change the session's sede activa), and a técnico still can't reach /sedes or /usuarios. Fixed one real brief bug found during this task: the brief's own script assumed a "Cerrar sesión" button would be visible right after the immediately-preceding forbidden-role redirect landed on /login, but that page has no sign-out button (it's the unauthenticated login form) -- removed the unnecessary click, since signIn() on the already-visible form overwrites the old session directly. 2/2 e2e (one correction attempt, per RULES.md #1), 379/379 unit suite, tsc clean.

======================================================================
FASE 6 (GESTIÓN DE SEDES): ALL 20 TASKS COMPLETE (2026-08-21)
======================================================================
All 20 tasks implemented (Tasks 1-3 via subagent-driven-development before the Agent-dispatch classifier began consistently blocking both implementer and reviewer dispatches; Tasks 4-20 executed and self-reviewed inline by the controller, same resolution as Fase 3 hit this same issue). 2 fix loops: Task 1's missing backfill regression test, and Task 20's brief bug (phantom sign-out button). 379/379 unit tests, 2/2 e2e (including the cross-sede isolation proof), tsc --noEmit clean.

## Final whole-branch review (2026-08-21)
Dispatched final review (opus) over the full Fase 6 diff (3b7c569..e5580ae). Eight cross-cutting checks performed (migration completeness -- 57 sede-aware call sites enumerated across the whole tree, all 39 unscoped ones verified reachable only after an already-scoped parent read; scope*-function consistency; ADMIN-bypass boundary; backfill correctness; e2e coverage honesty; login-flow security; cross-file semantic consistency; backward compatibility) -- all clean except one Critical:

1. CRITICAL - A session/JWT issued BEFORE this phase's deploy carries no `sedeActivaId` (the field didn't exist in the token shape before Fase 6). `scopeOrden(undefined)` returns `{ sedeId: undefined }`, and Prisma silently treats an `undefined` `where` field as "not provided" -- every sede filter across the app (listOrdenes, getOrden, updateBodegaAction, deleteRepuestoAction, all ten migrated action files) would silently degrade to tenant-wide instead of sede-scoped for any user still holding a pre-Fase-6 token. Nothing in requireSession()/requireRole() asserted the field's presence. Invisible in unit tests (which always construct a realistic session) and in e2e (which always logs in fresh) -- would only surface live the moment any tenant has a second sede, at which point it's the exact IDOR class this phase exists to close.
2. Important - the e2e cross-sede isolation proof only demonstrated read-side list-hiding (`/ordenes` shows no cross-sede link) -- never proved the actual IDOR boundary the code comments claim (`getOrden`'s `findFirst`, not `findUnique`) via a direct URL to a known cross-sede id.
3. Important (documented, not coded) - `Repuesto.codigo` is `@unique` tenant-wide (not per-bodega/sede), so a second sede cannot stock any part code the first sede already uses -- broader impact than just "blocks transfers" as originally framed; needs an explicit product decision (scope codigo per bodega or per sede) before any tenant is given a second sede. No code change in this phase; recorded here as the decision point.

FIXED in commit 498f1a0 (pushed to main):
- Critical: one guard in `requireSession()` (`src/lib/auth/guards.ts`) -- `if (!session.user.sedeActivaId) redirect("/login?error=sede-requerida")`, placed after the tenant-mismatch check, before returning the session. Forces a fresh login, which mints a token with a real sedeActivaId (Task 6's authorizeCredentials). New error copy in login-error-message.ts. Independently re-reviewed: `auth()` has exactly two importers repo-wide (guards.ts and the NextAuth route handler itself) -- requireSession() is a genuine, total chokepoint, confirmed by grep across all 17+ action files. Catches both `undefined` and a hypothetical empty string. No redirect loop (the target page is session-free). Does not regress a legitimate session (falsy check only).
- Important #2: e2e now captures the ABC123 orden's URL while still in Sede principal (right after confirming the heading renders, proving the URL is well-formed and resolves), then from the Sede-norte técnico session navigates directly to that exact URL and asserts a 404 -- re-derived independently as attributable only to getOrden's findFirst+scopeOrden miss (route is role-open to TECNICO, no other 404 source exists on it).
- Pre-existing guards.test.ts/requireRole tests needed `sedeActivaId` added to their fixture session objects (the new guard would otherwise redirect them before they tested what they originally tested) -- re-review confirmed this was honest fixture-only changes, no assertion weakened.
- 382/382 unit tests, 2/2 e2e, tsc clean.

**Ready to merge: Yes.**

Minor findings from the review NOT addressed (deferred to backlog, none blocking):
- `guards.test.ts` now has two byte-equivalent tests ("returns the session..." and "...when sedeActivaId is present") -- consider consolidating or repointing one at the empty-string branch specifically.
- `Session.user.sedeActivaId` is typed non-optional (`string`) while `JWT.sedeActivaId` is optional and bridged with an `as string` cast in auth.ts -- exactly the cast that hid the original bug from tsc. The runtime guard now closes the hole, but the type still lies; a future lint pass could flag the guard as dead code on a `string`. Typing the session field `string | undefined` (or dropping the cast) would let the compiler enforce it instead of just a comment.
- `Repuesto.codigo`'s tenant-wide uniqueness (finding #3 above) -- explicit product decision needed before any tenant gets a second sede; not a code defect in this phase's scope.
- `setUsuarioSedesAction` verifies every submitted sedeId exists but never verifies the usuarioId -- a bogus id produces a deleteMany count 0 + createMany FK error, no data damage, just an inconsistent-with-the-sede-check convention.
- An ADMIN can delete the sede they're currently logged into (all four deleteSedeAction pre-checks are satisfiable for a fresh empty sede an ADMIN bypassed into) -- reads go silently empty, writes hit a raw FK error until re-login. A fifth pre-check (`id === session.user.sedeActivaId`) would close it.
- Backfill's `ORDER BY created_at ASC LIMIT 1` has no secondary tiebreaker -- unreachable in practice (provisionTenant creates exactly one sede before any backfill would ever run against it), noted only given the SQL's stakes.
- Login's uniform "Correo, contraseña o sede incorrectos" message means picking the wrong (unassigned) sede is indistinguishable from a wrong password -- deliberate (prevents enumeration), but worth a UX note since every sede name is listed in the selector regardless of assignment.
- e2e's `page.once("dialog", ...)` before the sede-delete click never fires (no browser `confirm()` exists) -- harmless, defensive only.
- `reporte-actions.ts` is the one migrated file that hand-writes `orden: { sedeId }`/`sedeId` instead of importing scopeFactura/scopeOrden -- identical behavior today, risk is drift if either scope definition ever changes.

======================================================================
FASE 6 (GESTIÓN DE SEDES): COMPLETE, REVIEWED, READY TO MERGE (2026-08-21)
======================================================================
All 20 tasks + 1 review-driven fix round (1 Critical + 1 Important fixed, 1 Important documented-not-coded) implemented, reviewed, independently re-reviewed, and pushed to main. Final state: 382/382 unit tests passing, 2/2 e2e (including both the cross-sede isolation proof and the direct-URL IDOR-boundary proof), tsc --noEmit clean. No branch/PR (direct-to-main per established project convention). Next: Fase 7 (Agendamiento de citas + recordatorios de mantenimiento preventivo) per the design doc's roadmap.

## FASE 6 SUMMARY
- 20 tasks completed (final task commit 06dade4) + 1 review-fix commit (498f1a0)
- 382/382 unit tests passing, 2/2 e2e, tsc clean
- Activates the dormant Sede/sede_id data model built in Fases 2-5: UsuarioSede bridge table + backfill, sede activa in the session (chosen at login, ADMIN bypasses the assignment check but not the sede-exists check), Sede CRUD + read-only user/sede-assignment admin surfaces, and a full sweep of ten pre-existing action files converting every unscoped Prisma call to sede-scoped findFirst/updateMany/deleteMany
- 1 Critical (stale pre-Fase-6 JWTs silently losing all sede scoping) + 1 Important (e2e only proved list-hiding, not the actual IDOR boundary) found by the final whole-branch review, both fixed and independently re-verified; 1 Important (tenant-wide Repuesto.codigo uniqueness blocking parallel multi-sede catalogs) documented as a required product decision before any tenant gets a second sede, not coded in this phase
- Explicitly deferred by user decision, not gaps: Plan/maxSedes billing-tier enforcement (Fase 9), inventory transfers between sedes (blocked by the Repuesto.codigo constraint above), live sede-switching without re-login (would need this app's first SessionProvider)
- Technical debt documented: 8 Minor findings from the final review (duplicate test, JWT/Session type-honesty gap, unvalidated usuarioId in setUsuarioSedesAction, self-sede-deletion by ADMIN, backfill tiebreaker, login-message UX note, dead e2e dialog handler, reporte-actions.ts's un-DRY scope filter), plus all backlog carried from Fases 1-5
- Status: Fase 6 complete, ready for Fase 7

======================================================================
FASE 7 (CITAS + RECORDATORIOS DE MANTENIMIENTO PREVENTIVO): IN PROGRESS
======================================================================

Task 1: complete (commit 1e72ba4, subagent-driven, reviewed). prisma/tenant/schema.prisma gains EstadoCita/MotivoRecordatorio enums, back-relations on Usuario/Cliente/Vehiculo/Sede, and three new models -- Cita (own sedeId, matches OrdenTrabajo's pattern), ConfiguracionSmtp (singleton row, id literal "singleton", enforced by a DB CHECK constraint so "at most one row" is a database invariant not an app convention), RecordatorioEnviado (dedup/audit log, not a field on Vehiculo). Migration SQL hand-written to match. Reviewer: spec ✅, task quality Approved, 0 Critical, 1 Important flagged as plan-mandated (`configuracion_smtp` uses `@@map` singular instead of the phase's stated "tabla_en_plural" convention) -- accepted as a deliberate, defensible exception given the model is a true DB-enforced singleton (brief's own design decision #1); not a defect, no fix dispatched. 13/13 tests (11 pre-existing + 2 new), tsc clean.

Task 2: complete (commit 6c3ae5a, subagent-driven, reviewed). src/lib/sede/scope.ts: scopeCita(sedeActivaId) -- `{ sedeId: sedeActivaId }`, Prisma-type-free like its siblings (scopeOrden/scopeBodega/scopeRepuesto/scopeEntrada/scopeFactura), returns a fresh object each call. Reviewer: spec ✅, 0 issues, task quality Approved. 8/8 scope tests, tsc clean.

Task 3: complete (commit 47e217e, subagent-driven, reviewed). src/lib/crypto/secret-box.ts: AES-256-GCM cifrarSecreto/descifrarSecreto under SMTP_ENCRYPTION_KEY (env-only, no fallback, hard-fails on missing/malformed key), versioned "v1:iv:tag:cifrado" envelope, random 12-byte IV per call, tamper/wrong-key detection delegated entirely to Node's own GCM auth tag (no manual comparison code). src/lib/email/smtp-config.ts: CONFIGURACION_SMTP_ID="singleton", descifrarConfiguracionSmtp builds its return value by explicit field listing (not a spread), structurally excluding passwordCifrado/activo from ever leaking. Reviewer (higher scrutiny given security-sensitive code): spec ✅, 0 Critical, 0 Important -- checked specifically for IV reuse, decrypt-failure-returns-garbage paths, and plaintext/key leaking into errors/logs, none found. 1 Minor noted (key Buffer never explicitly zeroed post-use, accepted JS/Node limitation). 14/14 tests (11 crypto + 3 smtp-config; brief's own step comments said 12/15, harmless miscount matching the Fase 6 Task 9 precedent), tsc clean.

Task 4: complete (commit a311582, subagent-driven, reviewed). src/lib/validation/cita.ts: citaInputSchema (vehiculoId, fechaHora via refine+transform not z.coerce.date -- version-proof Spanish message, motivo, optional notas) + estadoCitaSchema (four-value enum). src/lib/validation/smtp.ts: smtpConfigInputSchema (host/usuario/fromEmail/fromNombre required, puerto coerced+range-checked 1-65535, password optional/empty tolerated for "keep stored" semantics, activo checkbox string->boolean transform). Reviewer: spec ✅, all Spanish error strings verified verbatim against the brief, 0 issues. 15/15 tests (8 cita + 7 smtp), tsc clean.

Task 5: complete (commit e9c08ff, subagent-driven, reviewed with elevated IDOR scrutiny). src/app/actions/cita-actions.ts: listCitas/getCita (requireSession, findFirst+scopeCita never findUnique), listVehiculosParaCita (deliberately unscoped -- Vehiculo is tenant-wide), createCitaAction/updateCitaAction (requireRole ADMIN+RECEPCION, guard called before FormData parsing in every action, clienteId derived from the resolved vehiculo never accepted from the client), cambiarEstadoCitaAction (requireRole ADMIN+RECEPCION), deleteCitaAction (requireRole ADMIN only, stricter than the other writes). All writes use updateMany/deleteMany+scopeCita+count===0 check, never update/delete-by-bare-id -- reviewer confirmed this is stricter than orden-actions.ts's own read-then-update-by-id pattern (Fase 6), not a regression. Reviewer: spec ✅, full function-by-function IDOR/scoping compliance table verified against the diff (not the report), 0 Critical, 0 Important (deleteCitaAction's missing friendlyPrismaErrorMessage wrap flagged but is plan-mandated, verbatim from brief, correctly not touched per RULES.md §7). 17/17 tests (brief's own prose said 18, harmless miscount), tsc clean.

**Execution order deviation, noted for the record:** Task 6 imports `enviarEmail` from Task 10, so per the plan's own Step 0 note, Tasks were reordered 9 -> 10 -> 6 -> 7 -> 8 -> 11 -> 12 -> 13 -> 14 instead of strict numeric order. Ledger entries below are kept in plan-numeric order regardless of execution order, for readability.

Task 9: complete (commit 4839519, subagent-driven, reviewed with hand-traced date-math scrutiny -- executed before Task 6/10 per dependency order). src/lib/recordatorios/mantenimiento.ts: UMBRAL_KM=5000/UMBRAL_MESES=6/COOLDOWN_RECORDATORIO_DIAS=90 as fixed module-scope constants (not configurable), sumarMeses (setUTCMonth overflow + setUTCDate(0) clamp-back, correctly handles Jan 31 + 1 month -> Feb 28), evaluarMantenimiento (km branch requires >=2 readings with non-null kilometraje and a dias>=1, non-positive-rate guard via kmRecorridos<=0; both fechaTiempo and fechaKm always computed then strict < picks the earlier one -- genuine "whichever comes first," not an if/else shortcut; bloqueadoPorCooldown computed independently of vencido, both can be true simultaneously). Zero imports, fully Prisma-free. Reviewer hand-traced all 6 rule scenarios (empty services, one service, non-positive rate, valid projection, tie-break, cooldown/vencido independence) against the diff: spec ✅, 0 Critical, 0 Important. 1 Minor noted (the dias<1 divide-by-zero guard is slightly stricter than the brief's literal wording, a defensible extra safeguard). 16/16 tests, tsc clean.

Task 10: complete (commit 2aa2aa8, INLINE execution -- Agent-dispatch classifier blocked the implementer subagent dispatch, same recurring issue as Fases 2-3/6, switched per user's standing consent; reviewer dispatch succeeded normally). src/lib/email/enviar-email.ts: the sole Nodemailer-aware module, transport created per call (not pooled -- each tenant has its own creds, and a pooled connection would keep decrypted passwords alive in memory), secure:true only on port 465 (implicit TLS), STARTTLS otherwise. src/lib/recordatorios/plantilla.ts: construirMensajeRecordatorio builds plain-text (zero markup, verified) + basic HTML with all customer-supplied fields (clienteNombre, vehiculo, tallerNombre) escaped via escaparHtml (all 5 HTML-significant chars, & first to avoid double-escaping) -- no templating engine, per scope. One real brief bug found and fixed (1 correction attempt, RULES.md §1): the brief's own mock `vi.fn(() => ...)` for mockCreateTransport failed tsc (TS2556) when spread-called with `unknown[]` args -- fixed by widening the mock's own signature to `vi.fn((..._args: unknown[]) => ...)`, no assertion changed. **New finding, not coded around, tracked for follow-up:** `npm install nodemailer` resolved to 8.0.11 (high-severity GHSA-p6gq-j5cr-w38f, CVSS 7.1, arbitrary file read/SSRF via the `raw` sendMail option) because @auth/core's peerDependency range (`^7.0.7 || ^8.0.5`) blocks the fixed 9.0.5 release. Reviewer independently confirmed `raw` is never passed anywhere in this codebase's own sendMail call (MensajeEmail has no such field), so today's exploitation vector is unreachable -- scoped Important not Critical. Follow-up: bump nodemailer to 9.x once @auth/core's peer range allows it; until then, `raw` must never be introduced into any enviarEmail call. Reviewer: spec ✅, 0 Critical, 1 Important (the CVE, tracked above), 2 Minor (unnecessary escaping of non-customer motivoTexto; placa unescaped in email subject header, not a real risk). 10/10 tests, tsc clean.

Task 6: complete (commit 22897a0, subagent-driven, reviewed with elevated secret-handling scrutiny -- executed after Task 10 per dependency order). src/app/actions/smtp-actions.ts, ADMIN-only throughout: getConfiguracionSmtp (never decrypts -- returns ConfiguracionSmtpVista with passwordConfigurada boolean flag only, no password field at all), guardarConfiguracionSmtpAction (empty submitted password keeps the existing stored ciphertext verbatim; refuses to save when no password is submitted AND no row exists yet, checked before any write), probarConfiguracionSmtpAction (test-destination email is pulled from session.user.email, never from FormData -- closes an open-relay vector; decrypts only inside the try block, catch discards the real error and returns one fixed generic message so no SMTP-auth-failure detail leaks). Reviewer verified secret-handling function-by-function against the diff (not the report): spec ✅, 0 Critical, 0 Important. 2 Minor (passwordConfigurada uses a length check instead of a stricter guard, unreachable in practice; bare catch in probarConfiguracionSmtpAction also swallows genuine bugs, a documented deliberate tradeoff). 13/13 tests (brief's own prose said 12, harmless miscount), tsc clean.

Task 7: complete (commit c11c905, INLINE execution -- Agent-dispatch classifier blocked the implementer subagent dispatch again, same recurring issue; reviewer dispatch succeeded normally). src/app/(dashboard)/citas/: page.tsx (list, Promise.all of listCitas+listVehiculosParaCita, "No hay citas agendadas en esta sede." empty state), nueva-cita-form.tsx (booking form, vehículo select labelled "{placa} — {marca} {modelo} ({clienteNombre})", empty-vehiculos state renders a message instead of a useless empty select), citas/[id]/page.tsx (detail -- calls the real next/navigation notFound() on a null cita, matching the exact established pattern from ordenes/clientes/facturas/vehiculos/entradas-mercancia detail pages, not a custom inline message -- this IS the direct-URL IDOR-boundary proof point Task 13 needs), cambiar-estado-cita-form.tsx (estado select bound via cambiarEstadoCitaAction.bind(null, citaId) to match its (id, prevState, formData) signature). Nav link added unguarded by role (listCitas uses requireSession() only). No test added for either page.tsx, per established Fase 5/6 RSC-page convention. Reviewer verified every UI assumption against Task 5's real action signatures/return shapes: spec ✅, 0 Critical, 0 Important, 0 Minor. One pre-existing unrelated flake hit during the full suite run (tenant-client.test.ts, parallel-schema-contention pattern already documented in the Fase 6 ledger) -- re-ran in isolation, 10/10 clean, 1 correction attempt per RULES.md §1. 5/5 new tests, 473/473 unit suite, tsc clean.

Task 8: complete (commit 8679889, subagent-driven, reviewed with elevated password-leak scrutiny). src/app/(dashboard)/configuracion-smtp/: page.tsx (ADMIN-gated purely server-side via getConfiguracionSmtp's own requireRole(["ADMIN"]), same no-client-guard convention as usuarios/page.tsx), configuracion-smtp-form.tsx (password `<input>` uses a literal defaultValue="" never conditioned on any server value -- ConfiguracionSmtpVista itself has no password field, so there is no value available to leak even by mistake; hint text tells the ADMIN a blank field keeps the existing password; second `<form>` wired to probarConfiguracionSmtpAction for the test-send button, gated on a configuracion already existing). Nav link uses the identical {esAdmin ? <Link>...</Link> : null} idiom as Reportes/Sedes/Usuarios. Reviewer verified the password-never-leaks property against both the form code and Task 6's ConfiguracionSmtpVista type: spec ✅, 0 Critical, 0 Important. 1 Minor (the "blank keeps existing" hint is gated on configuracion truthiness rather than the more semantically precise passwordConfigurada boolean field -- functionally equivalent today since guardarConfiguracionSmtpAction never persists an empty password, but leaves the boolean field unused). 6/6 new tests, 482/482 unit suite, tsc clean.

Task 11: complete (commit cfcbd9d, subagent-driven implementer, INLINE review -- Agent-dispatch classifier blocked the reviewer subagent dispatch this time, controller reviewed directly against the diff given this is the most business-critical task of the phase). src/lib/recordatorios/ejecutar-recordatorios.ts: zero Prisma imports (confirmed by reading the file's import lines directly), two-level try/catch -- outer per-tenant (a missing/inactive SMTP config or a decrypt failure increments tenantsSinSmtp/fallidos and continues to the next tenant, never throws past the loop) and inner per-vehicle (a failed enviarEmail is caught, counted in fallidos, loop continues to the next vehicle). registrarRecordatorio is called strictly after a successful enviarEmail await, never before and never on a caught failure -- confirmed by reading the control flow directly, not the report -- so a failed send stays retryable on the next sweep by design (explicit code comment). Cooldown-skip and no-email-skip paths both short-circuit before the inner try block, never touching enviarEmail/registrarRecordatorio. describirError() reports only the error's constructor name, never its message, since SMTP/crypto errors routinely embed hosts/usernames/credentials and this summary is returned over HTTP; MAX_ERRORES_REPORTADOS=50 caps the response size. src/lib/recordatorios/gateway-prisma.ts: listarVehiculosParaRecordatorio's ordenes sub-select uses estado:"ENTREGADA"+entregadaAt not null, orderBy entregadaAt desc, take 2 -- verified this exactly matches Task 9's "most-recent-first, at most two" servicios contract; Vehiculo.cliente is a required (non-optional) Prisma relation (confirmed in schema.prisma), so vehiculo.cliente.id has no null-pointer risk. One legitimate type-only fix (1 correction attempt, RULES.md §1): the brief's `ReturnType<typeof vi.fn>` mock annotations didn't typecheck under this repo's vitest@4.1.10, widened to `Mock` imported from vitest -- confirmed by diffing against the brief's original annotation, no assertion or mock behavior changed. Independently re-ran tests and tsc rather than trusting the implementer's report: 13/13 tests, tsc clean. 0 Critical, 0 Important. 1 Minor/informational noted: if registrarRecordatorio itself throws after a successful send, the vehicle is counted as fallido despite the email having actually sent, and the next sweep will retry it -- a documented, deliberate at-least-once-delivery tradeoff (favors a possible duplicate email over a silently-never-reminded customer), not a defect. No route/scheduler wiring in this task -- expected, Task 12 does that.

Task 12: complete (commit 0ee4087, subagent-driven implementer, INLINE review -- Agent-dispatch classifier blocked the reviewer subagent dispatch again, controller reviewed directly against the diff given this is the sweep's auth boundary). src/app/api/cron/recordatorios/route.ts: GET has no session/requireSession() by design (external scheduler caller). autorizado() fails closed on an unconfigured CRON_SECRET (checked before any comparison), hashes BOTH the received and expected secret via SHA-256 before timingSafeEqual (never a raw === comparison -- avoids both the timing side-channel and a length-mismatch throw that would itself leak the secret's length), rejects a missing/malformed Bearer header cleanly -- all confirmed by reading autorizado()'s actual code directly, not the report. No code path echoes the secret or the incoming header value in any response; the try/catch around ejecutarRecordatorios returns one generic 500 message on an unexpected throw (ejecutarRecordatorios already absorbs per-tenant/per-vehicle failures internally per Task 11, so reaching this catch means the sweep couldn't even start, e.g. publicDb unreachable). Deps wiring (listarTenants from publicDb.tenant.findMany({select:{schemaName:true}}), gateway: prismaRecordatoriosGateway, descifrarConfiguracion: descifrarConfiguracionSmtp, enviarEmail, ahora: new Date()) verified to match Task 11's EjecutarRecordatoriosDeps shape exactly, and Tenant.schemaName confirmed to exist in prisma/schema.prisma. Independently re-ran tests and tsc: 9/9 tests (including a dedicated leak-check asserting a raw connection-error message never reaches the 500 JSON body), tsc clean. .env.example gained CRON_SECRET documentation alongside SMTP_ENCRYPTION_KEY (a Task 3 gap, filled retroactively here). One pre-existing unrelated flake hit during the full-suite run (scripts/seed-tenant-user.test.ts, P1002 advisory-lock timeout -- same parallel-schema-contention pattern already documented in the Fase 6 ledger, different file than Task 7's flake) -- re-ran in isolation, 3/3 clean. 0 Critical, 0 Important, 0 Minor.

Task 13: complete (commit beedea8, subagent-driven across two dispatch rounds -- first implementer got BLOCKED correctly per RULES.md §1 after fixing two real bugs in the brief's own literal code and diagnosing a third without applying it; second dispatch applied that diagnosed fix and reached a real green run). e2e/global-setup.ts: RECEPCION user seeded via the same "grant the oldest existing sede" default path every seeded user gets (Sede norte doesn't exist yet at global-setup time, so this deterministically lands the RECEPCION user in Sede principal only). e2e/tenant-flow.spec.ts: RECEPCION books+confirms a cita in Sede principal; the detail URL is captured only AFTER the heading is confirmed visible (matching the Fase 6 precedent this task's own brief cites); ADMIN switches to Sede norte and the cita is proven both list-hidden AND unreachable by a direct page.goto(citaUrl) asserting a real HTTP 404 status (not page-body text) -- this IS the getCita findFirst+scopeCita IDOR boundary Task 7 built; ADMIN switches back to Sede principal and the same cita reappears, proving sede-filtering not deletion; /configuracion-smtp proven ADMIN-only both by nav-link absence and by direct-URL rejection to /login?error=forbidden. Two real bugs found and fixed in the brief's own transcribed code (RegExp passed to Playwright's string-only SelectOption.label; a login-then-goto race missing the same toHaveURL sync used everywhere else in the file) plus one diagnosed-and-fixed race (a second "Cerrar sesión" click firing from an authenticated /configuracion-smtp page needed the same toHaveURL(/\/login/) wait already used elsewhere in the file -- an earlier "Cerrar sesión" in the same block correctly does NOT need it, since it fires from an already-settled /login?error=forbidden page with no pending navigation to race; reviewer independently verified this distinction against the diff, not just the report). playwright.config.ts timeout raised 30s->90s (outside the brief's stated file list, called out explicitly) -- the single shared e2e test now walks ~25 routes across every fase and Next.js dev-mode per-route compilation was pushing total time past the old budget; scoped to just the timeout value, nothing else touched. Reviewer verified the IDOR-404 assertion and both SMTP-gate assertions directly against the diff: spec ✅, 0 Critical, 0 Important, 0 Minor. Controller independently re-ran the one unit test that failed immediately after the e2e run (scripts/provision-tenant.test.ts, DB-load flake from the preceding e2e run) in isolation: 13/13 clean. 2/2 e2e, tsc clean.

======================================================================
FASE 8 (NOTIFICACIONES AUTOMÁTICAS AL CLIENTE SOBRE ESTADO DE ORDEN): COMPLETE, REVIEWED, READY TO MERGE (2026-08-24)
======================================================================
Plan: docs/superpowers/plans/2026-08-24-torqueflow-phase8-notificaciones-orden.md (4 tasks). Executed inline (user's explicit choice this phase, to avoid the recurring Agent-dispatch classifier blocking issue seen in Fases 3/6), direct to main, no worktree/branch/PR.

Task 1: complete (commit d71ad81). prisma/tenant/schema.prisma gains `ResultadoNotificacionOrden` enum (ENVIADA, FALLO_ENVIO) and `NotificacionOrdenEnviada` model (ordenId/clienteId FKs onDelete Cascade, estado, emailDestino, resultado, enviadoAt) + back-relations on OrdenTrabajo/Cliente. Migration `20260824190000_add_notificacion_orden_enviada` hand-written to match, applied clean against the reference schema. 14/14 provision-tenant tests, tsc clean.

Task 2: complete (commit ef75994). src/lib/notificaciones/plantilla.ts: `esEstadoNotificable` type guard narrows `EstadoOrden` to `EstadoNotificable` ("EN_PROCESO"|"TERMINADA"|"ANULADA") -- BORRADOR has no incoming transition, ENTREGADA deliberately excluded (customer is already at the counter, a notification then is redundant). `construirMensajeEstadoOrden` builds plain-text + basic-HTML per estado, reusing the exact escaparHtml pattern from Fase 7's recordatorios/plantilla.ts. 8/8 tests, tsc clean.

Task 3: complete (commit d751be5). src/lib/notificaciones/enviar-notificacion-estado.ts: DB-free orchestrator over an injected `{ smtp: SmtpConfigDescifrada | null, enviarEmail }`, exactly like Fase 7's ejecutarRecordatorios -- decides ENVIADA/SIN_SMTP_ACTIVO/SIN_EMAIL_CLIENTE/FALLO_ENVIO/ESTADO_NO_NOTIFICABLE with zero Prisma imports. 5/5 tests, tsc clean.

Task 4: complete (commit ff441fe). updateEstadoOrdenAction (src/app/actions/orden-actions.ts) now attempts the notification strictly after its estado `update` commits: reads ConfiguracionSmtp, decrypts via Fase 7's descifrarConfiguracionSmtp, calls the Task 3 orchestrator, and best-effort-writes a NotificacionOrdenEnviada audit row (only for ENVIADA/FALLO_ENVIO -- SIN_SMTP_ACTIVO/SIN_EMAIL_CLIENTE are configuration facts surfaced live, never persisted). EstadoFormState gains a non-blocking `advertencia` field, rendered as role="status" in cambiar-estado-form.tsx (state.error stays reserved for the estado change itself failing). e2e/tenant-flow.spec.ts extended with one assertion (SMTP not yet configured at that point in the flow -> advertencia visible, estado change still succeeds). 18/18 new/changed action tests, 4/4 form tests, tsc clean.

## Final whole-branch review (2026-08-24)
Dispatched final review (opus) over the full Fase 8 diff (5a9db1f..ff441fe). Verified: integration placement (guard chokepoint + sede scoping untouched, notification strictly after the estado update+revalidatePath, no IDOR from the new `include:{cliente,vehiculo}`), Fase 7 SMTP reuse with zero duplication (only import sites, no new provider/secret/dependency/templating engine, no WhatsApp/SMS), audit-write best-effort honestly tested, and the 5-value TS `ResultadoNotificacion` union narrows correctly onto the 2-value persisted Prisma enum. 0 Critical. 1 Important:
1. `configuracionSmtp.findUnique` + `descifrarConfiguracionSmtp` in `notificarCambioEstadoOrden` ran fully unwrapped -- `descifrarConfiguracionSmtp` throws on a rotated/missing SMTP_ENCRYPTION_KEY or a corrupted ciphertext envelope (GCM auth-tag mismatch), and since this runs strictly after the estado `update` already committed, an uncaught throw here would have surfaced as an unhandled server-action error despite the estado change having succeeded -- the one path that violated the phase's own binding non-blocking-failure policy. Every other Fase 7 SMTP call site (smtp-actions.ts, the cron route) already wraps this same call; orden-actions.ts was the only unwrapped site in the codebase.

FIXED in commit a2c9b63 (pushed to main): the read+decrypt pair wrapped in its own try/catch, returning the same `ADVERTENCIA_POR_RESULTADO.FALLO_ENVIO` string (not a duplicated literal) a genuine send failure produces, with no audit row written (nothing was attempted, and `emailDestino` is NOT NULL -- a client with no email reaching this path would otherwise attempt a NULL insert). New test (`passwordCifrado: "basura"`) independently falsified by the re-reviewer: checked out the pre-fix file, confirmed the test fails with the real `descifrarSecreto` "Formato de secreto cifrado inválido" error escaping uncaught, restored the fix afterward. Independently re-reviewed (opus, adversarial): fix traced line-by-line as sufficient (both the async findUnique rejection and the synchronous decrypt throw are inside the try; every downstream call was already bounded), no regression to the four other previously-verified outcome paths, test confirmed real (not tautological -- decrypt fails on `secret-box.ts`'s own envelope-format guard, unrelated to any crypto-library version quirk). One noted, accepted behavior widening beyond the literal finding: the fix also now absorbs a genuine DB error on the `configuracionSmtp.findUnique` read itself (previously would have escaped), consistent with the phase's design decision, recorded here as a deliberate choice rather than an accident.

5 Minor findings from the review NOT addressed (deferred to backlog, none blocking): the audit-write catch (pre-existing) and the new decrypt-failure catch are both silent, unlike Fase 7's `ejecutarRecordatorios`/`route.ts` convention of logging the sanitized `error.constructor.name` -- means a rotated/corrupted SMTP_ENCRYPTION_KEY (the one failure mode that needs operator action) leaves zero server-side trace and is indistinguishable from an ordinary SMTP outage; `emailDestino: params.clienteEmail as string` cast in the audit-write is safe only by the current call ordering, prefer narrowing over casting; SMTP `activo:false` (row present but deactivated) has no dedicated test; `ADVERTENCIA_POR_RESULTADO` being `Partial<Record<...>>` means a future added `ResultadoNotificacion` member silently maps to no advertencia rather than failing the build; the e2e assertion is substring-coupled to the `SIN_SMTP_ACTIVO` string specifically (currently correct only because the orchestrator checks `smtp` before `clienteEmail`).

**Ready to merge: Yes.**

## FASE 8 SUMMARY
- 4 tasks completed (commit ff441fe) + 1 review-fix commit (a2c9b63, the sole Important finding fixed and independently re-verified)
- 525/528 unit tests pasando (3 skipped; el único fallo es el flake transitorio ya documentado de concurrencia de esquema compartido en tenant-client.test.ts/provision-tenant.test.ts, confirmado limpio en aislamiento en ambas revisiones), tsc --noEmit limpio
- Notificaciones por email integradas en updateEstadoOrdenAction: dispara para EN_PROCESO/TERMINADA/ANULADA (ENTREGADA excluida a propósito), estrictamente después de que el cambio de estado se confirma, nunca bloquea ni revierte el cambio de estado ante ningún fallo (SMTP no configurado, sin email, envío fallido, o ahora también fallo de descifrado)
- Reutiliza íntegramente la infraestructura SMTP de Fase 7: ConfiguracionSmtp singleton, descifrarConfiguracionSmtp/AES-256-GCM, enviarEmail (Nodemailer) -- cero duplicación, cero nuevo secreto, cero nueva dependencia
- Auditoría best-effort: tabla NotificacionOrdenEnviada registra solo los resultados ENVIADA/FALLO_ENVIO (SIN_SMTP_ACTIVO/SIN_EMAIL_CLIENTE se muestran en vivo vía `advertencia`, nunca se persisten); un fallo al escribir el registro nunca hace fallar la acción
- e2e extendido (e2e/tenant-flow.spec.ts) con una aserción sobre el flujo de cambio de estado existente
- Explícitamente fuera de alcance (decisión vinculante del usuario, no re-derivada del design doc): WhatsApp/SMS (diferido a Fase 10+), SendGrid/AWS SES/Resend o cualquier proveedor externo de email, motor de plantillas HTML complejo
- Deuda técnica documentada (5 Minor de la revisión final, ninguno bloqueante): ver el bloque de Minors arriba
- Status: Fase 8 complete, ready for Fase 9

======================================================================
FASE 9 (USUARIOS/ROLES + SUPER-ADMIN + PLANES DE SUSCRIPCIÓN): IN PROGRESS
======================================================================
Started 2026-08-24. Plan: docs/superpowers/plans/2026-08-24-torqueflow-phase9-usuarios-superadmin-planes.md (11 tasks). Started subagent-driven, direct to main (standing user consent, no worktree, same as every prior phase). Agent-dispatch classifier blocked a reviewer resume-dispatch on Task 1's fix round (same recurring issue as Fases 3/6) -- switched to inline for the remainder of the phase per the user's standing instruction, no further subagent attempts.

Task 1: complete (commits 8e70847, 0d49ecd -- subagent-driven implementer + inline-reviewed fix round). Public schema gains `Plan` (nombre unique, precio Decimal? nullable placeholder, maxUsuarios/maxSedes Int? -- NULL means sin límite práctico, never a sentinel) and `Tenant.planId`/`Tenant.estado`. Migration seeds the three fixed tiers (Básico 3/1, Estándar 10/1, Avanzado NULL/NULL) and backfills every pre-existing Tenant row to Avanzado via the mandated nullable->UPDATE->NOT NULL three-step shape, so no already-deployed tenant gets newly restricted. `provisionTenant` now assigns new tenants the Básico plan, looked up by `nombre` (not a hardcoded id) -- same pattern applied to a collateral fix in `public-client.test.ts`, which broke `tsc --noEmit` once `planId` became required (disclosed, not hidden). Task reviewer (sonnet): spec 100% compliant against the brief and all four locked design decisions (1-3 plus the seed values); 1 Important finding -- the new backfill-verification test asserted against "every tenant row except SCHEMA" with no isolation from other test files' concurrently-running fixture-provisioning `beforeAll`/`afterAll` blocks (this project's shared-DB test architecture, documented in vitest.config.ts), reproduced as a genuine intermittent-failure risk. FIXED (same implementer subagent): the test now snapshots pre-existing tenant ids before calling `provisionTenant` itself and asserts only against that snapshot, closing the race against tenants created during this test's own execution. Re-verified by the fix implementer: 16/16 alone, 16/16 clean twice more run together with the two sibling files the reviewer used to reproduce the original flake (one *unrelated* P1002 advisory-lock timeout hit each combined run, in a different file's `beforeAll` -- pre-existing `migrate deploy` contention, not from this fix). Re-review of the fix was attempted via subagent resume and blocked by the classifier; assessed inline instead: the fix directly closes the concrete race the reviewer demonstrated and is empirically clean under repeated concurrent runs -- approved. One residual, narrower theoretical race (a sibling fixture already alive at the exact snapshot moment, rather than one created afterward) was not chased further, matching this project's established "unreachable in practice, not pursued" tolerance for analogous backfill-test edge cases (see Fase 6's ORDER BY tiebreaker note). 1 Minor (fixed literal seed-row ids instead of cuid(), plan-mandated verbatim from the brief, non-functional) needs no action. tsc clean throughout.

Task 2: complete (commit b17ddf0, inline execution -- classifier blocked the Task 1 reviewer resume, switched to inline for the rest of the phase per standing user instruction). src/lib/planes/limites.ts: obtenerLimitesPlan(tenantSchema) -- the only function that reads publicDb for plan data, a plain two-hop lookup (Tenant -> Plan -> {maxUsuarios, maxSedes}), null passthrough unchanged (sin límite práctico, never a sentinel). createSedeAction (sede-actions.ts) now checks maxSedes before creating: skips the count query entirely when the limit is null (Avanzado), otherwise counts existing sedes and refuses with a Spanish message when at the limit. 17/17 tests (2 new limites.test.ts + 3 new + 12 pre-existing in sede-actions.test.ts, all green, mockSedeCount already existed in that file's fixture from Fase 6's deleteSedeAction tests -- reused, not duplicated), tsc clean.

Task 3: complete (commit 50fc935, inline). src/lib/validation/usuario.ts: usuarioCreateInputSchema (password required, min 8) + usuarioUpdateInputSchema (password optional, blank keeps the existing one -- same convention as Fase 7's ConfiguracionSmtp password field, first time applied to a tenant Usuario's own credential). 6/6 tests, tsc clean.

Task 4: complete (commit c2a45e7, inline). usuario-actions.ts gains createUsuarioAction (checks maxUsuarios via obtenerLimitesPlan before writing, bcrypt.hash at cost 12 matching seedTenantUser's existing convention), updateUsuarioAction (rehashes only on a non-blank password; refuses to demote the last ADMIN out of the role), deleteUsuarioAction (refuses to delete the last ADMIN; every other FK-Restrict relation -- órdenes/DVIs/facturas/pagos/historial/entradas/citas/mecánico -- relies on friendlyPrismaErrorMessage's existing generic P2003 message rather than eight separate pre-checks, per the plan's explicit proportionality decision). Test file's `usuario`/`obtenerLimitesPlan` mocks extended in place (matching the file's existing `ADMIN` session-fixture naming, not the brief's generic `SESSION` placeholder). 16/16 tests, tsc clean.

Task 5: complete (commit cebb507, inline). /usuarios/nuevo (NuevoUsuarioForm) + /usuarios/[id] (EditarUsuarioForm, notFound() on an unknown id, matching the established detail-page pattern) + "Crear usuario"/"Editar" links wired into the existing /usuarios list page, replacing its now-stale "usuarios se crean con npm run tenant:seed-user" copy. Two real bugs found and fixed during TDD, one correction attempt each (RULES.md §1): (1) native HTML5 required-field validation blocked React 19's server-action submission on an empty test click -- same class of bug Fase 1 Task 13 hit and fixed the same way, `<form noValidate>` while keeping `required` on the inputs; (2) a form action bound via `.bind(null, id)` (not through useActionState) is invoked with `(id, FormData)`, not just `(id)` -- fixed the test's own assertion, not the production code. Módulo 10 (usuarios/roles) now complete end to end. 9/9 tests across the three usuarios test files (no regression to the pre-existing AsignarSedesForm tests), tsc clean.

Task 6: complete (commit 43bc179, inline). Public schema gains SuperAdmin (email unique, password_hash, nombre). scripts/seed-super-admin.ts (+ CLI wrapper, npm run superadmin:seed) mirrors seedTenantUser's upsert-by-email idempotent pattern exactly. 2/2 tests, tsc clean.

Task 7: complete (commit b042914, inline). src/lib/super-admin/verify-credentials.ts mirrors src/lib/auth/verify-credentials.ts (bcrypt.compare against SuperAdmin). src/lib/super-admin/auth.ts: a second, fully independent NextAuth() instance -- basePath "/api/superadmin/auth", its own session cookie name "superadmin-session-token", own Credentials provider, route handler at src/app/api/superadmin/auth/[...nextauth]/route.ts. src/lib/super-admin/guards.ts: requireSuperAdmin() returns a local narrow SuperAdminSession {id,email,nombre}, never the ambient next-auth Session type. Verified against the installed next-auth@5.0.0-beta.32/@auth/core types before writing any code (node_modules/@auth/core/index.d.ts:522 basePath, types.d.ts:161 CookieOption{name,options}, node_modules/next-auth/lib/client.d.ts:86 SessionProviderProps.basePath) -- all three matched the plan's assumptions exactly, no API-surface guessing. One real type-honesty collision surfaced by tsc and fixed with a single documented `as User` cast in the authorize() callback: src/types/next-auth.d.ts's module augmentation of `User` is global/ambient and demands role/tenantSlug/tenantSchema/sedeActivaId even on this second instance's return value -- fabricating placeholder values for those fields would have been an active lie sitting in the JWT, so the mismatch is bridged with one cast at the exact NextAuth type boundary instead, with nothing downstream (guards.ts) ever trusting those fields. 5/5 tests, tsc clean.

Task 8: complete (commit a247596, inline). src/app/superadmin/layout.tsx wraps the route group in `<SessionProvider basePath="/api/superadmin/auth">` (verified SessionProviderProps.basePath exists in the installed next-auth/lib/client.d.ts, same verify-before-write discipline as Task 7) -- without it, next-auth/react's client signIn() would default to the tenant instance's /api/auth. superadmin-login-form.tsx mirrors the tenant LoginForm's shape (one generic "Correo o contraseña incorrectos" for every failure mode) but calls signIn without a sedeId field, matching the super-admin's flat identity. 2/2 tests, tsc clean.

Task 9: complete (commit 42bacc0, inline). resolveTenant()/ResolvedTenant gains estado; authorizeCredentials returns null for a SUSPENDIDO tenant with zero credential/DB calls made (same "no distinguishable failure mode" posture the login flow already had for wrong password/unknown email/wrong sede); requireSession() redirects an already-issued session to /login?error=tenant-suspendido the moment its tenant is found suspended mid-session, with a new login-error-message.ts entry. All three touched test files' existing mocks (none of which set `estado`) kept passing unmodified -- `tenant.estado === "SUSPENDIDO"` is simply false/undefined for every pre-existing fixture, exactly the non-breaking additive change the plan called for. 45/45 across the full auth+resolve-tenant test set, tsc clean. Módulo 11's suspend/reactivate mechanism is now enforced; Task 10 wires the super-admin UI that flips `estado`.

Task 10: complete (commit 5f4495f, inline). src/app/actions/super-admin-actions.ts: listTenantsConPlan/listPlanes (both requireSuperAdmin-gated reads) + cambiarEstadoTenantAction/cambiarPlanTenantAction. /superadmin/page.tsx: a plain table of every tenant (slug/estado/plan.nombre) with TenantRowActions per row -- a hidden-input estado-toggle form (labelled Suspender/Activar depending on current estado) and a plan <select>+Guardar-plan form, each its own useActionState. Módulo 11 (super-admin panel) now complete end to end: login (Task 8), suspend/reactivate enforcement (Task 9), and this dashboard close the loop. 574/575 full suite (the one failure, scripts/provision-tenant.test.ts's first test, is the already-documented shared-schema migrate-deploy contention flake -- confirmed 16/16 clean in isolation), tsc clean.

Task 11: complete (commits 3164c9e, 201812c, d32457e). Permanent dev tenant `taller-dev` created outside the plan's own scope, at the user's explicit request before Fase 9 resumed after a context reset: ADMIN admin@dev.test/Admin123!, "Sede principal", plan upgraded to Avanzado -- verified never targeted by e2e/global-teardown.ts (which only ever touches E2E_SLUG). e2e/super-admin-flow.spec.ts (new, own disposable tenant `taller-e2e-superadmin` to avoid a suspend/reactivate race against tenant-flow.spec.ts's shared tenant): login -> suspend -> confirm blocked login -> reactivate -> change plan -> confirm login works again. e2e/tenant-flow.spec.ts extended with a usuario create/edit/role-change/delete flow. e2e/global-setup.ts upgrades the shared E2E_SLUG tenant to Avanzado (the new Básico default's maxSedes=1 would otherwise block the pre-existing multi-sede flow).

A genuine pre-existing bug in **Fase 1**'s `src/app/login/login-form.tsx` blocked reliable e2e verification: NextAuth's credentials callback always responds HTTP 200 even on wrong credentials (`{url:"...?error=CredentialsSignin"}` in the body, never a 4xx -- confirmed by direct curl/browser reproduction, including the `X-Auth-Return-Redirect` header next-auth/react's signIn() sends). `SignInResponse.ok` reflects only HTTP status, never auth success; `!result.ok` is therefore always false and the check silently never fires. The identical defect was independently found and fixed in this phase's own superadmin-login-form.tsx. Per RULES.md §7 this is out of Fase 9's scope -- **the user was asked explicitly and approved a one-off exception** ("Corrígelo ahora (excepción puntual)"). Fixed in its own clearly-labeled commit (3164c9e, `fase1-fix:` not `fase9-task`), same fix shape (`result?.error` instead of `!result?.ok`) applied to both forms, test mocks in both `*.test.tsx` files corrected from the previously-tautological `{ok:false}` shape to the real `{ok:true, error:"CredentialsSignin"}`/`{ok:true, error:undefined}` shapes.

A residual intermittent e2e flake surfaced only under Playwright's concurrent 2-worker execution (never in isolation): `tenant-flow.spec.ts` occasionally hits a late-flow `?error=forbidden` redirect, root-caused as the same class of Turbopack cold-route-compile race already mitigated elsewhere in this file (warm-up fetches, `page.waitForResponse()` over `networkidle`, exact/filtered locators). After exhausting the legitimate mitigation attempts allowed under RULES.md §1 (max 1 correction attempt per error), **the user explicitly accepted this as documented technical debt** and instructed not to keep iterating on it -- both specs are proven correct in isolation (2/2), the flake is infrastructure timing under load, not application logic.

## Final whole-branch review + fix round (2026-08-24)

Dispatched final review (opus) over the full Fase 9 diff (a5a169b..d32457e). Verdict: 0 Critical, 4 Important, 7 Minor, initial "Ready to merge: No" (blocked on I1 and I3).

- **I1 (fixed, commit 201812c):** `createUsuarioAction` created new usuarios with zero `UsuarioSede` grants -- a TECNICO/RECEPCION with no sede grant cannot pass the sede gate in `authorizeCredentials`, so every user created through the new UI was unable to log in. Fixed by granting the tenant's oldest sede on create, mirroring `seedTenantUser`'s existing day-one grant. Two new tests.
- **I2 (owner decision: documented as debt, not fixed):** role demotions/deletions don't invalidate an already-issued JWT -- a demoted ADMIN keeps acting as ADMIN until session expiry. The reviewer flagged this as "the owner's call" because it directly conflicts with Fase 6's own explicit ledger precedent ("accepted stale JWT roles for v1"), re-raised here because Fase 9 is the first phase whose entire purpose is revoking access via UI. **Presented to the user directly; decision: document as accepted technical debt, consistent with the Fase 6 precedent.** Confirmed via re-review that Fase 9's own new SUSPENDIDO-tenant check is NOT subject to the same staleness (checked fresh per-request in `resolveTenant`/`requireSession`, never JWT-cached) -- only the pre-existing role-staleness pattern is being carried forward unchanged.
- **I3 (fixed, commit 201812c):** the backfill-invariant test in `provision-tenant.test.ts` snapshotted tenant ids at test-start, which breaks if any other process creates a tenant between the migration and test start (happened in practice from manual debug tenants created this session). Rewritten to identify pre-migration tenants by `createdAt < planAvanzado.createdAt` -- provably sound, since the `planes` table and its Avanzado row are created inside the same migration.
- **I4 (fixed, commit 201812c):** `listTenantsConPlan`, `listPlanes`, `cambiarEstadoTenantAction`, `cambiarPlanTenantAction` had no test proving they actually call `requireSuperAdmin()` before touching the database. Four new rejection tests added.

Fix round independently re-reviewed (opus, confirmation pass against the same diff): all four findings verified resolved or correctly deferred, no new Critical/Important introduced by the fix commits, full suite green (579 passed, 3 skipped -- the one pre-existing shared-schema `migrate deploy` contention flake, clean in isolation).

**Ready to merge: Yes.**

7 Minor findings from the review NOT addressed (documented as backlog debt, none blocking):
1. `createUsuarioAction`'s new sede-grant test coverage is mock-based call-shape assertion only -- no test logs in end-to-end as a freshly created user (the sede-gate logic itself is separately covered in `authorize-credentials.test.ts`).
2. The migration's three seeded Plan rows use fixed literal ids instead of `cuid()` -- plan-mandated verbatim from the brief, non-functional.
3. Neither the audit-write catch nor the SMTP-decrypt-failure catch pattern established in Fase 8 was revisited in Fase 9's own new code paths for consistency of error logging.
4. `deleteUsuarioAction` relies on `friendlyPrismaErrorMessage`'s generic P2003 message for all eight FK-Restrict relations rather than per-relation pre-checks -- plan's explicit proportionality decision, not a gap.
5. Super-admin dashboard has no pagination or search over the tenant table -- acceptable at current scale, would need revisiting before a large tenant count.
6. `Plan.precio` is a nullable placeholder with no UI to set or display it yet -- explicitly deferred per the confirmed design-decision answer ("precio placeholder nullable").
7. I2 itself (see above) remains open technical debt, explicitly accepted rather than silently missed.

## FASE 9 SUMMARY
- 11 tareas completadas (commits 8e70847..d32457e) + 1 ronda de corrección post-revisión (201812c, resolviendo I1/I3/I4) + 1 fix aprobado de Fase 1 fuera de alcance (3164c9e, excepción puntual autorizada por el usuario)
- Bug de `login-form.tsx` (Fase 1) corregido: NextAuth responde HTTP 200 incluso con credenciales incorrectas; `!result.ok` nunca detectaba el fallo. Corregido en ambos formularios (tenant y super-admin) con `result?.error`
- Tenant permanente `taller-dev` creado (ADMIN admin@dev.test, Sede principal, plan Avanzado), verificado fuera del alcance de cualquier script de teardown de e2e
- 579/582 tests unitarios pasando (3 skipped; el único fallo restante es el flake ya documentado de contención de `migrate deploy` sobre esquema compartido, limpio en aislamiento) + 2/2 e2e en aislamiento, tsc --noEmit limpio
- Flake intermitente de concurrencia (Turbopack + 2 workers de Playwright) documentado como deuda técnica de infraestructura, no de lógica -- aceptado explícitamente por el usuario, sin más iteración
- Módulo 10 (usuarios/roles CRUD), módulo 11 (panel super-admin: listar/suspender/reactivar tenants, asignar plan), y entidad `Plan` en `public` con enforcement solo numérico (maxUsuarios/maxSedes, sin feature flags) -- completos end to end
- Revisión final (opus) + ronda de corrección: 0 Critical, 4 Important (3 corregidos, 1 documentado como deuda por decisión explícita del dueño del producto), 7 Minor documentados como backlog
- Status: Fase 9 complete, ready to merge, ready for Fase 10
